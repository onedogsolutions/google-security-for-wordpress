# State Tracker - Google Security for WordPress

## Current Phase: Phase 36 (conflict handling becomes warn-only)

### Phase 36 Modifications (v2.18.1)
- **Operator decision, reversing the Phase 35 sub-decision.** Phase 35 kept suppression as the behaviour of the recommended `active` mode and relied on loud warnings to make it survivable. The operator has since chosen **warn only**: this plugin no longer removes another plugin's reCAPTCHA in the recommended configuration.
- **Only `site` mode suppresses now.** `GSWP_Conflict_Guard` registers its `script_loader_tag` filter only when the mode is `site`; `off` and `active` are both non-destructive and hook nothing. `our_recaptcha_active()` had no callers left and was removed. `GSWP_Recaptcha_Loader::is_suppressing()` reduces to `'site' === $mode`.
- **`off` and `active` are now behaviourally identical.** Stated plainly rather than disguised: dedup and warnings are unconditional in every mode, so with suppression gone from `active` there is nothing left to distinguish the two. Both stored values are retained so no migration is needed, and `active` stays the recommended value per the operator's earlier decision. The UI relabels the modes to describe what they actually do — "Share one loader" (recommended) and "Remove other plugins' reCAPTCHA" (marked destructive) — with `off` described as a legacy equivalent.
- **The Critical warning state is now coherent.** It can only arise when an operator has deliberately selected the destructive mode, so a non-dismissible notice is proportionate rather than something the plugin inflicts by default. All Critical-state copy (admin notice, plugins-row notice, REST diagnostic) now names the setting responsible and tells the operator how to switch it off.
- **Residual risk from Phase 35 is closed for the default configuration.** With divergent keys in the recommended mode, both loaders now load. Only one site key can be pre-rendered, so one of the two will still fail to execute — that is unchanged and unfixable without the multi-key `grecaptcha.enterprise.render()` path — but this plugin no longer *causes* the other plugin's failure, it reports the situation and leaves it intact.
- `tests/manual/09-recaptcha-coexistence.php` gains assertions that a divergent-key loader survives in every mode except `site`, and that `is_suppressing()` tracks the mode. Browser scenarios 3 and 4a/4b updated: scenario 3 (matching key, `site` mode) now expects **nothing** suppressed, since a matching key is shared rather than removed even there.
- No option schema change and no migration. `php -l` clean on all touched PHP; `npm run lint:js` at its pre-existing baseline of 35 errors in the untouched portion of `Diagnostics.jsx`. Webpack rebuilt.
- **Still not verified on staging** — Phase 35's browser scenarios remain outstanding, and scenario 4b now requires deliberately selecting the destructive mode to reach the Critical state.
- Version bumped to 2.18.1 (main file header, `GSWP_VERSION`, `readme.txt` stable tag + changelog, `package.json`, `package-lock.json`).

## Historical Phase: Phase 35 (reCAPTCHA loader ownership and conflict corrections)

### Phase 35 Modifications (v2.18.0)
Implements items A2–A7 of `PLAN-recaptcha-loader-corrections.md`. Closes findings S2, S3, S4 and the loader half of the Phase 33 incident. S5a (Gravity Forms' own assessments carrying no transactionData) is **not** addressed here — see §6.2 of the analysis plan.

- **Root cause fixed: nothing owned the page's reCAPTCHA loader.** New `includes/class-gswp-recaptcha-loader.php` is the single owner. The handle `google-recaptcha-v3` was previously registered from three places with independently duplicated logic (`GSWP_Assets`, `GSWP_Frontend`, `GSWP_Blocks_Integration`); all three now delegate to it.
- **Deduplication at render time.** `filter_tag()` on `script_loader_tag` (priority 5, ahead of the Conflict Guard at 10) emits the *first* loader for a given site key and drops later duplicates. Order-independent: whichever tag prints first satisfies every consumer, since `grecaptcha` is a global. On the operator's site — Gravity Forms and this plugin sharing one Enterprise key — this collapses two tags into one and both plugins keep working.
  - **Deviation from the plan.** §3.3 specified registering a src-less alias when a foreign loader already provides our key. Implemented differently: the handle is always registered *with* its src (so the dependency graph resolves for `GSWP_Blocks_Integration`, which lists it as a dependency) and the duplicate is dropped at tag-render time instead. This avoids an ordering hazard — deciding at enqueue time would require knowing whether another plugin registers before or after us, which is not knowable at priority 20.
- **Detection is generic, not Gravity Forms specific.** `foreign_loaders()` walks `wp_scripts()->registered`, matches the src against the reCAPTCHA needles, and parses the `render=` parameter for the site key. No third-party option keys or handles are consulted, so WPForms, Elementor, Fluent Forms and CF7 are covered without naming them. `render=explicit` and keyless loaders are correctly treated as non-conflicting.
- **Deferral deleted (S2).** `should_defer()`, `is_form_rendered()`, `is_recaptcha_configured()`, `is_gf_handle()` and `$gf_handles` are gone; `GSWP_Gravity_Forms` reduces to `is_active()`. The `registered`-means-rendered bug, the `post_content` string sniffing, and the speculative handle list all go with them.
- **Print/enforce agreement (A4).** `GSWP_Recaptcha_Loader::will_load()` is the single predicate, reading only `gswp_site_key`. Field printing (`GSWP_Frontend`, `GSWP_Login`, `GSWP_Powerpack`) and server-side enforcement (`GSWP_Verifier::verify_token()`) both call it. Because it depends on stored options rather than page state, the render request (GET) and the validation request (POST) always agree — which is what makes the gate safe. A request-scoped predicate would let a caller opt out by omitting a field, i.e. the bypass class removed in 2.17.0.
- **Bootstrap moved to the footer (A2.2).** Previously attached via `wp_add_inline_script()` to our own handle, which silently dropped the script when the handle was unregistered — one of the failure modes behind the 2.16.0 outage — and would not survive our tag being deduplicated. Now printed once on `wp_print_footer_scripts` (priority 20), guarded on `typeof grecaptcha` with a bounded poll (100 ms interval, 10 s ceiling) so it works whichever plugin's loader tag won. The two previously divergent bootstraps (`GSWP_Assets` generic + `GSWP_Frontend` WooCommerce-aware) are unified into one; the WooCommerce-aware version is a superset and is inert without WooCommerce.
- **DOM kill switches deleted (S4).** The `document.querySelector('.gform_wrapper, .gf-form, [id^="gform_"]')` bail-outs in both bootstraps are gone.
- **Conflict Guard narrowed (S3).** The blanket "Gravity Forms is installed, do nothing" escape is deleted. It now never suppresses a loader whose `render` key matches ours, and the Gravity Forms handles were removed from the handle-only match list — a handle with no src cannot be checked for which key it carries, so matching on handle alone risked suppressing a shareable loader blind.
- **Divergent-key reporting.** Two severities: **Warning** (both loaders present, not suppressed — dismissible) and **Critical** (we are actively suppressing another plugin's loader — not dismissible while it persists). Five surfaces: site-wide admin notice on any `manage_options` screen, a plugins-screen row notice in the Critical state, the Compatibility settings panel, the log (`error` level when suppressing, `warning` otherwise), and a new `loader_conflicts` section in `POST /gswp/v1/diagnose`.
  - Detection happens on the front end but the warnings live in wp-admin, so observations are bridged by the `gswp_foreign_recaptcha` transient (7-day TTL), written **only when the observed set changes** — the common case is one `get_transient()` per request and no write.
  - Notice dismissal is stored as the **conflict hash** in user meta (`gswp_loader_conflict_dismissed`), so a changed conflict — different plugin, different key, or suppression newly turning on — re-arms the notice instead of staying dismissed forever.
- **Operator decision honoured:** `active` remains the Recommended conflict mode. Its copy now states that it only applies to differently-keyed loaders. **Residual risk, unchanged and deliberate:** in `active`/`site` mode with divergent keys we still suppress, and the other plugin's forms may still fail. The warnings make that visible, not harmless. The safer variant (warn only, never suppress) remains a one-line change in `GSWP_Conflict_Guard::filter_tag()`.
- **No new options and no migration.** Sites on `active` stay on `active`; its behaviour simply becomes correct for matching keys. Persistent artefacts are one transient and one user meta key, both inert if the code is absent, so a downgrade to 2.17.0 is a plugin-file swap.
- **Testing.** New `tests/manual/09-recaptcha-coexistence.php` (read-only; asserts key parsing, dedup, matching-key survival through the Conflict Guard, print/enforce agreement, and hash re-arming) plus a 9-scenario browser checklist for staging. `php -l` clean on all touched PHP; `npm run lint:js` back to its pre-existing baseline of 35 errors, all in the untouched portion of `Diagnostics.jsx`. Webpack rebuilt.
- **Known limitation — wp-login.php is outside the dedup path.** `GSWP_Login::print_scripts()` prints its own `<script src>` tag directly on `login_footer` rather than going through `wp_register_script()`, so it never reaches the `script_loader_tag` filter and is not deduplicated. Left as-is deliberately: it is a self-contained, working path (it carries no Gravity Forms kill switch and never did), other plugins rarely load reCAPTCHA on wp-login.php, and rewriting it would put the login screen at risk for no observed benefit. Revisit if a conflict is ever reported there.
- **Not yet verified on staging.** The browser scenarios in the test script — in particular scenario 7 (WooCommerce Blocks checkout with a foreign loader present, where the handle must stay registered as a script dependency) and 4b (Critical-state warnings) — have not been run. Scenario 4b needs a second Enterprise site key.
- Version bumped to 2.18.0 (main file header, `GSWP_VERSION`, `readme.txt` stable tag + changelog, `package.json`, `package-lock.json`).

## Historical Phase: Phase 34 (checkout verification bypass removal)

### Phase 34 Modifications (v2.17.0)
- **Problem (security).** Phase 33 added `GSWP_Gravity_Forms::is_form_submission()` — a bare `isset( $_POST['gform_submit'] )` — and used it in `GSWP_Verifier::validate_checkout()` to skip verification for "Gravity Forms submissions". The predicate reads a client-supplied POST field and was not gated on Gravity Forms being installed, on a valid form ID, or on a GF nonce. Adding `gform_submit=1` to any WooCommerce classic checkout POST skipped the reCAPTCHA score check, the expected-action check, Transaction Defense scoring and `transactionData`, the `gswp_txn_block` high-risk block, and the `gswp_checkout_blocked` alert — on every install, Gravity Forms present or not.
- **The guard was also inert for its stated purpose.** `woocommerce_after_checkout_validation` fires from `WC_Checkout::process_checkout()`. A Gravity Forms submission never invokes it, so the bypass could not have fired for a legitimate GF submission — only for a crafted WooCommerce checkout POST. Removing it therefore restores checkout scoring with no change to Gravity Forms behaviour and no regression risk for normal traffic (an ordinary Woo checkout never posts `gform_submit`).
- **Fix.** Deleted the early return in `GSWP_Verifier::validate_checkout()` and removed `GSWP_Gravity_Forms::is_form_submission()` entirely (it had no other caller).
- **Scope.** This is item A1 of `PLAN-gravity-forms-stripe-and-integration-architecture.md`, shipped on its own because the hole is live in 2.16.0. The remaining Phase 33 defects documented in that plan are **not** addressed here and remain open:
  - `is_form_rendered()` still treats a merely *registered* GF script handle as proof a form is on the page, so `should_defer()` can disable our asset pipeline on pages with no Gravity Form while `inject_recaptcha_field()` still prints an empty token field — those submissions then fail closed with "verification token is missing".
  - `GSWP_Conflict_Guard::filter_tag()` still returns early whenever Gravity Forms is merely installed, disabling suppression sitewide while the Compatibility tab still shows it as active.
  - The `document.querySelector('.gform_wrapper, .gf-form, [id^="gform_"]')` bail-outs in `GSWP_Frontend` and `GSWP_Assets` are still present and still page-scoped.
  - No site-key arbitration exists; the underlying dual-loader cause of the GF/Stripe failure is unfixed.
- Version bumped to 2.17.0 (main file header, `GSWP_VERSION`, `readme.txt` stable tag + changelog, `package.json`, `package-lock.json`). No `src/` changes, so no webpack rebuild required. `php -l` clean on both touched files.

## Historical Phase: Phase 33 (Gravity Forms reCAPTCHA deferral)

### Phase 33 Modifications (v2.16.0)
- **Problem:** on pages where Gravity Forms renders its own Enterprise v3 reCAPTCHA (e.g. a GF Stripe checkout), our plugin's Conflict Guard and/or dual reCAPTCHA Enterprise script loading broke GF's form initialization. The Stripe payment link element (`#stripe-payment-link`) never mounted because GF's reCAPTCHA-dependent rendering failed. Disabling our plugin resolved the issue.
- **Fix — automatic deferral to Gravity Forms.** New static utility class `includes/class-gswp-gravity-forms.php` detects when GF has reCAPTCHA configured (`gravityformsaddon_recaptcha_settings` option with a non-empty `site_key` or `public_key`) AND a form is rendered on the current page. When both conditions are met, `GSWP_Gravity_Forms::should_defer()` returns true and:
  1. `GSWP_Frontend::register_scripts()` bails early — our `enterprise.js` never loads.
  2. `GSWP_Assets::enqueue_api_script()` returns false — all integration paths (Xootix, PowerPack) are blocked.
  3. `GSWP_Conflict_Guard::filter_tag()` returns the original tag — GF's scripts are never suppressed.
  4. `GSWP_Verifier::validate_checkout()` skips token validation when `$_POST['gform_submit']` is present — GF handles its own server-side verification.
- **Timing-safe detection (revision 2).** Initial implementation cached the `should_defer()` result; when called early at priority 10, GF had not yet enqueued its scripts, so the cached `false` caused the Conflict Guard to suppress GF's reCAPTCHA anyway. Fixes:
  - Removed per-request caching — `should_defer()` re-evaluates on each call so it catches GF's scripts once enqueued later in the lifecycle.
  - `GSWP_Frontend::register_scripts()` moved to priority 20 on `wp_enqueue_scripts` (after GF's default 10).
  - `is_form_rendered()` now checks both `enqueued` AND `registered` script states, additional handles (`gravityforms`), and content-based detection for `gform_wrapper` / `gf-form` markup (covers widgets, templates, GF routing).
  - `is_recaptcha_configured()` checks both `site_key` and `public_key` (varies by GF version).
  - Conflict Guard gains a handle-based whitelist (`is_gf_handle()`) and a src-based safety net: when GF has reCAPTCHA configured, any script whose src matches `google.com/recaptcha` is never suppressed — catches GF's loader even under an unrecognized handle.
- **Admin UI.** Informational note added to `Compatibility.jsx` under the conflict-mode radio panel explaining the automatic GF deferral.
- Version bumped to 2.16.0 (main file header, `GSWP_VERSION`, `readme.txt` stable tag + changelog, `package.json`, `package-lock.json`). Webpack rebuilt.

## Historical Phase: Phase 32 (API Diagnostics tool)

### Phase 32 Modifications (v2.15.0)
- **Problem:** errors appearing in Google Cloud Console with no visibility into what the plugin is actually sending to Google's reCAPTCHA Enterprise API. Administrators had no way to audit payloads or verify connectivity without enabling verbose logging and reading WooCommerce logs.
- **Fix — API Diagnostics endpoint and settings UI.** New admin-only REST route `POST /gswp/v1/diagnose` (in `class-gswp-rest-api.php`) performs live test assessments against Google's API and returns structured results:
  1. **Configuration check** — validates all required credentials are present (site key, GCP project ID, API key) with masked values.
  2. **Connectivity test** — sends a dummy token to the Enterprise assessments API (or classic siteverify). HTTP 200 with `tokenProperties.invalidReason=TOKEN_NOT_FOUND` proves credentials and API access are correct; non-200 reveals the exact error (400 = bad payload, 403 = bad key/permissions, 404 = wrong project).
  3. **Account Defender test** — builds and sends a real `userInfo` payload (accountId hash, createAccountTime, optional email via `gswp_ad_share_email`) exactly as the verifier does during login, showing whether Google accepts the structure and whether `accountDefenderAssessment` labels are returned.
  4. **Transaction Defense test** — builds and sends a full `transactionData` payload (billing/shipping address, items, payment method, `fraudPrevention: ENABLED`) matching a real checkout, verifying Google accepts it and showing whether `fraudPreventionAssessment` is returned.
- **Settings UI.** New `Diagnostics.jsx` React component added to the Enterprise Defense tab. "Run Diagnostic" button triggers the endpoint; results display per-test status cards with expandable request/response JSON (API key redacted in URLs).
- **No new options.** Uses existing credentials and feature toggles. Diagnostic is admin-only (`manage_options`).
- Version bumped to 2.15.0 (main file header, `GSWP_VERSION`, `readme.txt` stable tag + changelog, `package.json`, `package-lock.json`). Webpack rebuilt.

## Historical Phase: Phase 31 (Local content-heuristic registration screening)

### Phase 31 Modifications (v2.14.0)
- **Problem:** the "Block suspicious sign-ups" toggle (`gswp_ad_block_signup`) only blocked registrations when Google's Account Defender returned the `SUSPICIOUS_ACCOUNT_CREATION` label. Bots submitting obvious gibberish fields (e.g. First Name: `XFMHSwoqvlpPhPaPgOMvGX`, Last Name: `QBraWnDOrDXaDpZZRYNFU`) passed through because labels require the console-side "Configure Account defense" enablement and a trained model — without those, `screen_registration()` saw empty labels and allowed the signup regardless of the toggle.
- **Fix — local content-heuristic screening.** New static `GSWP_Account_Defender::screen_registration_content( $fields, $email, $source )` analyses submitted `first_name`, `last_name`, and `user_url` fields for unambiguously machine-generated patterns. A field is flagged when any one of three signals fires: (1) fewer than 20% vowels in a 10+ alpha-char string; (2) 8+ consecutive consonants; (3) 12+ alpha chars with no spaces and 5+ upper/lower case transitions. The registration is **blocked when 2 or more fields** are flagged (requiring multiple fields avoids false positives on a single unusual-but-legitimate value). Gated by the existing `gswp_ad_block_signup` toggle — no new option. Does NOT require an Enterprise key or Account Defender to be active (the patterns are unambiguous regardless of key type).
- **Alert integration.** Fires the existing `gswp_suspicious_registration` action with labels `['CONTENT_HEURISTIC']` and context `'source' => $source, 'blocked' => true`, so the Phase 20/27 alert pipeline emails the operator identically to a label-based block.
- **Wired into all four registration surfaces** after the existing score + label checks pass: `GSWP_Powerpack::validate_registration()` (reads `$userdata['first_name']`/`['last_name']`/`['user_url']`), `GSWP_Login::validate_register()` (reads `$_POST` — core's form lacks name fields but themes/plugins may add them), `GSWP_Xootix::validate_register()` (reads `$_POST`), and `GSWP_Verifier::validate_registration()` (WooCommerce — reads `$_POST['first_name']`/`['last_name']`/`['website']`).
- **False-positive safety verified:** real names ("John", "Smith", "Bjorn", "Ng", "Xi Jinping") and normal URLs never trigger; the observed spam data triggers on 2+ fields and is blocked.
- **Processing overhead:** negligible — pure in-memory string operations (regex + character loop) on 2–3 short fields; no network, database, or crypto. Several orders of magnitude less than the reCAPTCHA API round-trip that precedes it.
- No new options, REST routes, React components, or MainWP changes. No webpack rebuild required (no `src/` changes).
- Version bumped to 2.14.0 (main file header, `GSWP_VERSION`, `readme.txt` stable tag + changelog, `package.json`, `package-lock.json`). `php -l` clean on all touched PHP.

## Historical Phase: Phase 30 (WP-CLI fatal error fix — unconditional class loading)

### Phase 30 Modifications (v2.13.2)
- **Fatal error under WP-CLI:** `Uncaught Error: Class "GSWP_Admin" not found` at line 463 of `google-security-for-wordpress.php`. Root cause: `class-gswp-admin.php` and `class-gswp-frontend.php` were conditionally `require`d based on `is_admin()` at plugin file-load time (lines 105–109), but the classes were instantiated inside `gswp_init()` on `plugins_loaded` using a *second* `is_admin()` check. During WP-CLI boot, `is_admin()` can return `false` at file-include time (so only the Frontend class was loaded) but `true` by the time `plugins_loaded` fires — causing `new GSWP_Admin()` to reference a class that was never required.
- **Fix:** Both `class-gswp-admin.php` and `class-gswp-frontend.php` are now loaded unconditionally (matching the pattern already used by all other classes in the plugin: Login, REST API, MainWP, Two-Factor, etc.). The conditional *instantiation* in `gswp_init()` remains unchanged — only the appropriate class is created based on context at hook time.
- Version bumped to 2.13.2 (main file header, `GSWP_VERSION`, `readme.txt` stable tag + changelog, `package.json`, `package-lock.json`).

## Historical Phase: Phase 29 (2FA popup focus on wp-login.php / LoginPress)

### Phase 29 Modifications (v2.13.1)
- Fixed the 2FA challenge popup losing keyboard focus on full-page-reload logins: native wp-login.php and skins built on it (LoginPress). Root cause: wp-login.php's core `wp_attempt_focus()` focuses and selects `#user_login` on a **200ms `setTimeout`**, which fires after the modal's on-open focus (the `requestAnimationFrame` retry pattern runs within ~2 frames, ≈32ms) and steals focus. AJAX logins (PowerPack, Xootix Login/Signup Popup) never reload the page, so nothing competed there — which is why focus already worked on those forms.
- Fix in `assets/js/gswp-2fa-modal.js`: a timing race can't be won by focusing earlier, so the dialog now **traps focus while open** — a document-level `focusin` listener (added in `open()`, removed in `close()`) refocuses the code input whenever focus lands outside the overlay. This defeats core's timer (its `d.focus()` fires `focusin` synchronously; the following `d.select()` doesn't move focus), any other plugin's focus script, and also keeps Tab from wandering behind the overlay. The on-open rAF focus is kept for the initial placement.
- Bumped version to 2.13.1 (main file header, `GSWP_VERSION`, `readme.txt` stable tag + changelog, `package.json`, `package-lock.json`) so the cache-busting query arg forces caches to fetch the new modal script. Plain asset — no webpack rebuild required.
- Provenance: this fix originated on the retired `charming-goodall-0vntw8` branch (authored against the then-current v2.2.3) during the old-branch cleanup; the JS change was ported unchanged onto v2.13.0 `main`, and the stale branch was retired once the fix landed here.

## Historical Phase: Phase 28 (Password Defense — native PHP leaked-credential detection)

### Phase 28 Modifications (v2.13.0)
- **Where this came from.** The Fraud Defense console's other outstanding recommendation ("Detect
  leaked passwords — Configure Password defense") is a sibling feature to Account Defender (closed
  out in Phase 27) that the plugin did not implement at all. Google's console only offers Java/Node
  client helpers for it; PHP is "Other," pointed at the raw protocol spec
  (`docs.cloud.google.com/recaptcha/docs/check-passwords#cryptographic-function`). Full
  investigation and design in `PLAN-password-defense.md`.
- **Native PHP implementation of Google's privacy-preserving Password Check protocol** — no
  dependency on Node or an external client. Three new `includes/` classes:
  - `class-gswp-scrypt.php`: pure-PHP RFC 7914 scrypt (PBKDF2 legs via native `hash_pbkdf2()`;
    ROMix/BlockMix/Salsa20/8 in PHP). Verified byte-for-byte against both official RFC 7914 test
    vectors and the exact scrypt(N=4096,r=8,p=1) parameters the protocol uses.
  - `class-gswp-ec-cipher.php`: NIST P-256 point arithmetic (GMP preferred, BCMath fallback) —
    scalar multiplication, point compression, and the SHA-256 "random oracle" hash-to-curve
    function from Google's `private-join-and-compute` `ECCommutativeCipher`. Implemented from the
    published algorithm (not ported/translated — Apache-2.0 vs. this project's GPLv2-or-later,
    see licensing note in the class docblock) and validated against it as an external test oracle.
  - `class-gswp-password-defense.php`: orchestrator — capability gating, the standalone
    `privatePasswordLeakVerification` assessment call (no token/event required), local verdict
    computation, per-user weekly cadence, and the login/choice-time hooks.
- **Verification methodology (unusually deep for this codebase, warranted by the stakes of silent
  crypto bugs — the plan's own §4.1 warns that a wrong canonicalization or hash-to-curve step
  produces silent false negatives, not errors).** Built php8.4-gmp/bcmath from PHP's own upstream
  source (`ext/gmp`, `ext/bcmath` via `phpize`) since neither extension ships in this sandbox, then:
  1. Fetched Google's official `recaptcha-password-check-helpers` npm package (Apache-2.0) and its
     underlying `private-join-and-compute` C++ source (`ec_commutative_cipher.cc`, `ec_group.cc`,
     `context.cc`) as read-only reference/oracle — never copied into this project.
  2. Generated real test vectors by executing the official helper's WASM `EcCommutativeCipher`
     (hash-to-curve, encrypt, decrypt, reencrypt with fixed keys) and its `CryptoHelper`
     (canonicalization, username hash/prefix, scrypt pair hash) via Node.
  3. Wrote a Python prototype of the full protocol (point arithmetic, hash-to-curve, scrypt) from
     the published algorithm and confirmed it reproduces the official helper's vectors exactly.
  4. Ported to PHP (both the GMP and BCMath backends) and re-verified against the same vectors.
     This caught two real bugs before they could ship: **(a)** the hardcoded P-256 curve-order
     constant was missing a trailing hex digit, making it composite instead of prime and silently
     breaking every modular inverse (decrypt/reencrypt); **(b)** BCMath's `bcmod()` keeps the
     dividend's sign (unlike `gmp_mod()`, which always returns non-negative), so the BCMath
     backend's point arithmetic produced garbage on the first negative intermediate value until a
     manual sign-fold was added. Both backends now reproduce Google's real WASM implementation
     exactly across hash-to-curve, encrypt/decrypt, and a simulated two-party
     blind/reencrypt/strip-and-verify round trip, plus a full leak-detected / not-leaked
     end-to-end simulation.
  5. Confirmed the canonicalization quirk from the plan (Google's own helper's
     `.replace('.', '')` strips only the *first* dot despite the docs saying "stripping dots," and
     the credentials-pair scrypt hash uses the **raw**, not canonicalized, username) by reading the
     helper's `crypto_helper.ts` source directly, and matched both exactly.
- **Login path (deferred, at most weekly per user).** `authenticate` filter at priority 45 (after
  the core password check at 20 and Account Defender's capture at 40, before 2FA at 100): on a
  correct password where a per-user cadence meta (`gswp_pd_last_check`, a `{time, fp}` pair keyed
  to a `wp_hash()` of the *already-hashed* stored password so no new secret material is persisted)
  says a check is due, the credentials are stashed and checked on `shutdown` — after the response
  has been sent, so the scrypt hash and API round trip never add login latency. A detected leak
  stores `gswp_pd_leaked` meta and fires `gswp_leaked_credentials`. On a later login with the same
  (still-leaked) password, the plugin either refuses the login with a link to the lost-password
  screen (opt-in `gswp_pd_force_reset`, default off) or lets it through with a persistent
  `admin_notices` nag (default) — never a surprise lockout. The flag clears itself once the
  password's fingerprint changes (password reset, profile change, or WooCommerce account-details
  change).
- **Choice-time surfaces (inline, may block).** `validate_password_reset`,
  `user_profile_update_errors`, and `woocommerce_save_account_details_errors` each run the same
  check synchronously against the newly submitted password (these POSTs are rare, so the ~0.3–1.5s
  scrypt cost is acceptable inline) and, under `gswp_pd_block_choice` (default on), add a blocking
  `WP_Error` so the form re-renders with an inline message instead of accepting a known-leaked
  password. With blocking off, the event is only logged and can still alert.
  **Scoped out of this pass** (documented, not silently dropped): the WooCommerce/PowerPack/Xootix
  *registration* password fields are not yet checked — those forms' password handling varies
  per-integration (Woo can auto-generate one) and would need the same source-verification rigor
  Phase 26 used for the PowerPack registration module; left as a follow-up rather than guessed at.
- **Alerts.** Fourth event type on the existing throttled pipeline: `gswp_leaked_credentials` →
  `GSWP_Alerts::on_leaked_credentials()`, gated by new `gswp_alert_leak` (default on), dedupe key
  `leak_{md5(login|ip)}` on the pipeline's default (non-login) 1-hour window, `format_leak()` /
  `leak_detail_lines()`, and the digest subject/section extended to four event types.
- **Settings.** New options: `gswp_password_defense` (master, default off, Enterprise-only),
  `gswp_pd_login` (default on), `gswp_pd_block_choice` (default on), `gswp_pd_force_reset` (default
  off), `gswp_alert_leak` (default on) — wired through `gswp_default_options()`, REST
  `get_settings`/`update_settings` (plus a read-only `pd_supported` flag: `PHP_INT_SIZE >= 8` and
  either `gmp` or `bcmath` loaded, so the UI can explain an inert toggle instead of guessing), the
  `App.jsx` defaults, and a new `src/components/PasswordDefense.jsx` panel on the Enterprise
  Defense tab (master + three sub-toggles, an Enterprise-required notice, an unsupported-server
  notice, and privacy copy naming exactly what leaves the site). `AlertSettings.jsx` gained a
  fourth "Leaked credentials" sub-toggle. MainWP bridge picks all of this up automatically (single-
  source REST validation, per the established pattern).
- **Fail-open throughout.** Any crypto exception, missing GMP/BCMath, missing credentials, or
  non-200/malformed API response returns `null` from `check_credentials()` and is treated as "not
  leaked, don't block" — a Password Defense outage or misconfiguration can never lock out or block
  a legitimate user.
- **Licensing note.** This project is GPLv2-or-later; Google's helper packages are Apache-2.0
  (incompatible with GPL-only, and mixing under "or later" would muddy WordPress.org distribution).
  Per the plan, the implementation is written from the publicly documented protocol; the official
  helper and the `private-join-and-compute` C++ source were used only as an external, read-only
  test oracle during development — never copied, ported, or translated — and this is noted in the
  new classes' docblocks.
- Version bumped to 2.13.0 (main header, `GSWP_VERSION`, `readme.txt` stable tag + Password
  Defense feature bullet + Email Alerts bullet update + new "Why does the console recommend
  Configure Password defense?" FAQ + changelog, `package.json`, `package-lock.json`). React assets
  rebuilt (`npm run build`); `wp-scripts lint-js` clean on all touched JSX; `php -l` clean on all
  touched/new PHP.
- **Not yet done (follow-ups, not silently skipped):** registration-surface password checks
  (WooCommerce/PowerPack/Xootix sign-up forms with a user-chosen password); the verifier-seam
  optimization to fold the leak check into an already-in-flight token assessment and halve API
  calls on choice-time surfaces; a live staging-site end-to-end run against Google's documented
  leaked test credential pair (this pass's verification is a from-scratch reimplementation checked
  against Google's *own* official client as an oracle — thorough, but not the same as an assessment
  actually round-tripping through the live reCAPTCHA Enterprise API); Slack/webhook delivery for
  the new alert type.

## Historical Phase: Phase 27 (Account Defender registration feedback loop + suspicious sign-up controls)

### Phase 27 Modifications (v2.12.0)
- **Reported (onedog.solutions, morning after 2.11.0):** two more spam registrations overnight (same gibberish-fields + Gmail dot-trick signature), and the Google Cloud **Fraud Defense** console still recommending "Configure Account defense" despite the four documented Account Defender pieces being coded. Investigation first (committed as `PLAN-account-defender-registration-feedback.md`): the console recommendation is a **console-side per-key enablement the plugin cannot perform** (operator must click it — new readme FAQ); the spam got through because blocking was **score-only** (default 0.5) with `SUSPICIOUS_ACCOUNT_CREATION` labels never read at registration time, registrations were **never annotated** (no fake-signup feedback loop at all), and the Gmail dot-trick made every alias hash to a **different accountId**, blinding Google's clustering.
- **Registration outcome feedback loop (`GSWP_Account_Defender`).** New `user_register` hook stores the just-made registration assessment (context-checked via the verifier's new `get_last_context()`) in `gswp_registration_assessment` user meta (`META_REGISTRATION`, `{name, time}`); the account's **first genuine login** (`wp_login` → `maybe_annotate_registration_outcome()`) consumes it and annotates `LEGITIMATE`; **deletion while the meta is still present** (`delete_user`/`wpmu_delete_user` → `on_user_deleted()`) — i.e. the account never logged in, exactly an operator sweeping spam — annotates `FRAUDULENT`. A request-static `$registered_user_id` guard stops an auto-login riding the registration request from settling its own signup. One hook covers all four registration surfaces since they all funnel through `wp_insert_user()`; admin-created users have no assessment in flight so nothing is stored. Active whenever Account Defender is on — no new toggle (this is the missing half of the existing feature).
- **Registration-time label screening.** New static `GSWP_Account_Defender::screen_registration( $verifier, $email, $source )` called after a **passing** `verify_token()` from all four registration validators (PowerPack `validate_registration` → module-shaped `wp_send_json_error`; core `GSWP_Login::validate_register`, WooCommerce `GSWP_Verifier::validate_registration`, Xootix `validate_register` → errors object). Logs labels (risk-only by default, all under verbose), fires new action **`gswp_suspicious_registration`** ($email, $labels, {source, blocked, assessment}) when `SUSPICIOUS_ACCOUNT_CREATION` is present, and — only under new opt-in **`gswp_ad_block_signup`** (default `0`) — returns a `WP_Error` so no user is created. Opt-in because labels only flow once the console feature is enabled and trained.
- **Mis-annotation fix.** `capture_login_assessment()` now only captures when `get_last_context()` ∈ {`login`,`wp_login`} — an auto-login after registration (wp_signon in the same request) previously captured the *registration* assessment and `wp_login` annotated it `LEGITIMATE + CORRECT_PASSWORD`, actively training Google that spam signups were legitimate.
- **Gmail alias normalization (`GSWP_Verifier`).** New `normalize_email_identifier()` used by `resolve_account_id()` for the email-keyed (registration) hash: lowercase; for gmail.com/googlemail.com strip local-part dots, strip `+tag`, fold googlemail→gmail. All dot-variants of one inbox now map to one accountId, restoring related-account clustering against the observed attack. Trade-off (documented in changelog): a pre-existing gmail registration hash changes once — acceptable since the console feature is only now being enabled.
- **Optional email sharing.** New opt-in **`gswp_ad_share_email`** (default `0`): when on, `build_account_user_info()` adds `userInfo.userIds = [{email}]` (resolved via new `resolve_identifier_email()`) per Google's recommendation for better detection; settings copy states plainly that raw addresses go to Google.
- **Alerts.** `GSWP_Alerts` gains the third event: subscriber for `gswp_suspicious_registration` gated by new **`gswp_alert_registration`** (default `1`), dedupe key `registration_{md5(email|ip)}` (1 h window via the existing default branch), riding the unchanged throttle/digest pipeline; new `format_registration()` / `registration_detail_lines()`, digest partitions/subject/sections extended to three types. Alert body tells the operator that deleting a spam account also reports it to Google.
- **Verifier context + logging.** New `$last_context`/`get_last_context()` (set at the top of `verify_token()`); `GSWP_Verifier::log()` now falls back to `error_log` under `WP_DEBUG` (mirroring the Account Defender helper) so fail-open skips are no longer invisible on non-WooCommerce sites.
- **Settings wiring** for the three new options through the standard chain: `gswp_default_options()`, REST `get_settings`/toggles, admin localizer, `App.jsx` defaults, `AccountDefender.jsx` (two new rows: "Block suspicious sign-ups", "Send email identifiers to Google"; panel copy now names the console-side Configure Account defense step), `AlertSettings.jsx` (third "Suspicious sign-up" sub-toggle). MainWP bridge picks them up automatically (single-source REST validation).
- **Operator checklist (no code, from the plan):** click **Configure Account defense** in the Fraud Defense console for the keys; verify Enterprise key + Account Defender toggle + WordPress-registration toggle on the site; raise `threshold_wp_register` to ~0.7; check the console for the overnight `register` assessments' scores; delete the spam users (now feeds FRAUDULENT annotations).
- Version bumped to 2.12.0 (main header, `GSWP_VERSION`, `readme.txt` stable tag + Account Defender/Alerts feature bullets + "Why does the Google Cloud console still recommend Configure Account defense?" FAQ + changelog, `package.json`, `package-lock.json` root). React assets rebuilt (`npm run build`); `php -l` clean on all touched PHP. See `PLAN-account-defender-registration-feedback.md` for the full investigation.

## Historical Phase: Phase 26 (Block spam registrations via the PowerPack Registration Form)

### Phase 26 Modifications (v2.11.0)
- **Problem reported by the user (agency site `onedog.solutions`):** recurring spam user registrations, shown via the site's "New User Registration" admin email — gibberish First/Last/Website values and a Gmail dot-trick address (`de.lg.a.dod.an.i.e.l.bc@gmail.com`, i.e. `delgadodanielbc@gmail.com` with dots inserted so each signup looks unique to WordPress while all mail lands in one inbox). Asked whether the plugin should block these.
- **Root cause — an uncovered registration surface.** The registration is built with Beaver Builder + the PowerPack "**Registration Form**" module, which renders its own form and submits over admin-ajax (`pp_register_user`), firing **neither** the WordPress core `register_form` / `registration_errors` hooks **nor** the WooCommerce ones. So none of the plugin's registration protections reached it: `gswp_enable_wp_register` hooks core `registration_errors` (`class-gswp-login.php:49-52`), `gswp_enable_registration` is WooCommerce, and the existing PowerPack integration (`class-gswp-powerpack.php`) only covered the *Login Form* module (`pp_login_form_process_login_errors` + the `pp_lf_process_lost_pass` guard). The email template itself confirmed the module (its `pp_rf_*` merge-tag admin notification, not core's "New user registration" notice).
- **Verified against source before coding.** Plan written first (`PLAN-spam-registration-blocking.md`, committed), then every PowerPack identifier re-verified against the paid **bb-powerpack 2.42.3** module source (`modules/pp-registration-form/`) — module class `PPRegistrationFormModule`, ajax action `pp_register_user`, the `pp_rf_form_end` render action and `pp_rf_before_user_register` pre-insert action, the `wp_send_json_error( array('code','message') )` error shape the module JS renders, the `user_email` field, and that it serializes with `FormData` on `.pp-registration-form`.
- **Fix — extend `GSWP_Powerpack` (PHP-only; no new class file).** Under the existing `class_exists( 'BB_PowerPack' )` gate, a new `gswp_enable_wp_register` block:
  - **Injects** the token field on the module's own `pp_rf_form_end` action (fires inside the `<form>` before it closes, analogous to the Xootix `xoo_el_form_end` integration): `inject_registration_field()` prints the hidden `g-recaptcha-response` field with `data-recaptcha-action="register"` and loads the shared API script + refresh bootstrap. Because the module serializes with `FormData`, the field is posted with no client-side change — no HTML-regex injection needed.
  - **Validates** on the module's `pp_rf_before_user_register` action (fires after the module's nonce + field validation, immediately before `wp_insert_user()`): `validate_registration()` runs `verify_token( 'wp_register', 'register', array(), $userdata['user_email'] )` and, on `WP_Error`, `wp_send_json_error( array( 'code' => 'gswp_recaptcha', 'message' => … ) )` — halting the request so **no user is created**, with the module rendering our message inline (unknown code falls through to `error.message`). A bot POSTing straight to admin-ajax without a token is rejected by the verifier's missing-token branch.
  - **Strips** the module's own reCAPTCHA: `replace_module_recaptcha()` now dispatches on module class and also removes the Registration Form module's `data-field-type="recaptcha"` field block + dequeues `g-recaptcha`. Not cosmetic — the Conflict Guard already suppresses the `g-recaptcha` handle (`class-gswp-conflict-guard.php:50`), so an un-stripped module captcha would render but stay inert and the module's client-side gating would block submit for everyone. The `fl_builder_render_module_content` strip filter is now registered once when login **or** registration is on, gated per module inside the method.
- **No new options / settings surface.** Reuses `gswp_enable_wp_register` + `threshold_wp_register` (the "third-party forms reuse the WP core toggles" convention already used by Xootix/PowerPack-login). No `gswp_default_options()`, `src/`, REST, or MainWP-bridge changes. Account Defender coverage is free: `wp_register` is already whitelisted for `accountId` (`class-gswp-verifier.php:897`), so passing the email seeds fake-signup labels on Enterprise keys.
- **Compatibility note.** `pp_rf_before_user_register` is `@since` PowerPack 2.32; the target site runs 2.42.3, so it is present. For older PowerPack the documented fallback is a priority-1 `wp_ajax_pp_register_user` guard (the lost-password pattern) — not wired, since it's unneeded here.
- **Inertness.** With PowerPack absent, or the register toggle off, or no Registration Form module on a page, nothing fires — the two actions and the strip filter never match. Login/lost-password behaviour is byte-unchanged (regression-guarded by the per-module gate in `replace_module_recaptcha()`).
- **Verification.** `php -l` clean on `includes/class-gswp-powerpack.php`. `npm install && npm run build` regenerated `build/` (no `src/` change this phase — reproduces the existing admin screen under the new version) so the distribution ZIP carries the compiled admin assets. Full end-to-end (a live WordPress + Beaver Builder + PowerPack staging site: real human registration passes, low-score/no-token blocked, module captcha stripped under Conflict Guard) still needs the operator's staging site — noted to the user.
- **Distribution ZIP** assembled from the same explicit file list as Phase 23/24 — `google-security-for-wordpress.php`, `readme.txt`, `includes/*.php`, `assets/`, `build/` — excluding `src/`, `tests/`, `node_modules/`, `.git/`, and the planning docs. No new file in `includes/` (edits are inside the already-shipped `class-gswp-powerpack.php`).
- Version bumped to 2.11.0 (main header, `GSWP_VERSION`, `readme.txt` stable tag + changelog, `package.json`). See `PLAN-spam-registration-blocking.md` for the full spec and the source-verified PowerPack internals.

## Historical Phase: Phase 25 (Bind 2FA enrolment to its origin site)

### Phase 25 Modifications (v2.10.0)
- **Bug reported by the user:** on `maddogproducts.com`, a live user had 2FA enrolled via Google Authenticator. After cloning the site to `staging.maddogproducts.com`, the *same* authenticator code logged into the staging clone. The user asked whether this was expected given reCAPTCHA covering both domains, or a bug to fix.
- **Root cause (documented, not a plugin defect per se, but worth closing):** a TOTP code is `TOTP(secret, time)` — no domain binding at all. Cloning a site copies `wp_usermeta` verbatim, including `gswp_2fa_secret`, so the clone holds an identical secret and produces identical codes. reCAPTCHA is an unrelated subsystem (bot-scoring via site keys) with zero bearing on TOTP; its domain coverage neither causes nor excuses this. The real risk is cross-environment **replay** — the plugin's existing replay guard (`META_LAST_TS`) is per-database, so a code observed on a lower-trust staging box is still valid against production within its time-step. Full analysis and design written up first as `PLAN-2fa-environment-binding.md` (committed separately) before any code changed, per the user's request to review before proceeding.
- **Fix — origin-host binding.** New `GSWP_Two_Factor::META_ORIGIN` (`gswp_2fa_origin`) user meta records the normalized host (`current_site_origin()`: `home_url()`'s host, lowercased, `www.` stripped, scheme/port ignored, filterable via `gswp_2fa_site_origin` for multisite/reverse-proxy setups) a secret was enrolled on, stamped in `save_profile()` on successful enrolment and cleared in `disable_for_user()`. `secret_is_foreign()` compares it against the current site; `user_has_2fa()` — the single choke point every enforcement/challenge/profile-UI path already calls — now returns `false` whenever the secret is foreign, so a cloned code simply stops working.
- **Fail-open by design.** A foreign secret never blocks a login outright: `enforce_second_factor()` treats the user as unenrolled and falls through to password-only sign-in (or, for enforced roles, `maybe_enforce_setup()` routes them into fresh re-enrolment). This is deliberate — a legitimate production domain migration must never lock every admin out because their old secret's stamped origin no longer matches.
- **Grace-period interaction.** Grace-period state is copied by a clone too, so a stale, already-expired `META_GRACE_START` could otherwise cause an instant lockout for an enforced role the moment the foreign-secret check flips them back to "unenrolled" on the new site. New `maybe_reset_grace_for_moved_secret()` clears the grace clock once per detected origin mismatch (tracked via a new `META_GRACE_RESET_ORIGIN` meta, so a persistent site move doesn't reset the clock forever and create a permanent bypass) — called from `maybe_enforce_setup()` right before the existing grace-deadline check.
- **Grandfathering + backfill.** A secret with no recorded origin (pre-existing enrolments) is treated as not-foreign until stamped, so nothing breaks immediately on upgrade. New `gswp_backfill_2fa_origin()` in the main file, called from the existing version-gated `gswp_maybe_migrate()`, stamps the current site as the origin for every enrolled user missing one. Documented limitation: this only protects clones made *after* the upgrade — if the upgrade happens to run on an already-existing clone first, that clone stamps itself as its own origin. The reported `staging.maddogproducts.com` site needs a one-time manual fix (reset/re-enrol 2FA there) since it predates this change; called out explicitly in the new readme FAQ.
- **New setting** `gswp_2fa_env_binding` (default `'1'`, on), added to `gswp_default_options()`, `env_binding_enabled()` (mirrors `remember_enabled()`, tied to the master 2FA switch), wired through REST `get_settings`/`update_settings` (`tfa_env_binding`, boolean toggle, same pattern as `tfa_remember`), the `App.jsx` default, and a new "Disable 2FA on cloned or moved sites" toggle row in `src/components/TwoFactorNotice.jsx` (placed after the "Remember this browser" section) explaining the staging-clone scenario and that turning it off restores the old cross-copy behaviour.
- No REST route, class list, or distribution-ZIP file-list changes — all edits are inside already-shipped files.
- Docs: new readme feature bullet, a "Why did 2FA reset after I cloned my site?" FAQ (explicitly separates this from reCAPTCHA, explains the fail-open behaviour, and calls out that a database clone still copies password hashes/secrets regardless — clone hygiene is still the operator's responsibility), and a changelog entry.
- Version bumped to 2.10.0 (main header, `GSWP_VERSION`, `readme.txt` stable tag + feature bullet + FAQ + changelog, `package.json`, `package-lock.json` root) — minor bump since a new user-facing option was added. React assets require a rebuild (`npm run build`) for the new JSX toggle; `php -l` clean on all touched PHP.

## Historical Phase: Phase 24 (Fix stale duplicate plugin install left after upgrade)

### Phase 24 Modifications (v2.9.1)
- **Bug (reported after installing 2.9.0 on a live site):** the Plugins screen showed two "Google Security for WordPress" rows — an old inactive version (e.g. 2.2.1) alongside the new active 2.9.0. Root cause: WordPress identifies a plugin by its **folder path** (`plugin_basename`), not its display name. When a ZIP is uploaded and the existing `wp-content/plugins/google-security-for-wordpress` folder can't be overwritten in place, WordPress extracts it into a differently-slugged sibling folder instead — invisible to the normal in-place upgrade, so the old folder is never replaced or cleaned up. (The MainWP dashboard extension's `MIN_GSWP_VERSION` constant, suspected at first, is unrelated — it's pure display logic on the MainWP-dashboard side with no bearing on how a child site installs a plugin ZIP.)
- **Fix.** New `gswp_remove_duplicate_installs()` in `google-security-for-wordpress.php`, generalizing the existing `gswp_remove_legacy_plugin()` pattern (which already retires the pre-rebrand "Google reCAPTCHA v3 for WooCommerce" plugin the same way): scans `get_plugins()` for any basename other than `self` whose `Name` is `'Google Security for WordPress'` or `TextDomain` is `'google-security-for-wordpress'`, then `deactivate_plugins( …, true )` (silent — skips the duplicate's own deactivation hooks) + `delete_plugins( … )`. Called from `gswp_activate()` (so a fresh install/reactivation cleans up immediately) **and** hooked to `load-plugins.php` (so a site already stuck on an older version — like the one that reported this — gets swept the next time it visits the Plugins screen, with no deactivate/reactivate cycle required). No persistent "already ran" flag needed: once the duplicate is deleted, `get_plugins()` simply finds nothing on the next sweep, so it's naturally idempotent and cheap (the Plugins screen already calls `get_plugins()` to render itself).
- No REST route, option, or other class changes. PHP-only; no React/webpack rebuild.
- Version bumped to 2.9.1 (main header, `GSWP_VERSION`, `readme.txt` stable tag + changelog, `package.json`, `package-lock.json` root). `php -l` clean on `google-security-for-wordpress.php`.

## Historical Phase: Phase 23 (MainWP child-side settings bridge)

### Phase 23 Modifications (v2.9.0)
- **MainWP child-side bridge.** New `includes/class-gswp-mainwp-child.php` (`GSWP_MainWP_Child`, constructed in `gswp_init()` right after `GSWP_Rest_Api`). Lets the already-shipped companion dashboard extension (`onedogsolutions/mainwp-for-google-security-for-wordpress` v1.0.0) read and write each child site's settings from a MainWP dashboard. The dashboard end already exists and currently resolves to its "bridge missing" state on every child because nothing hooked `mainwp_child_extra_execution` yet; this phase closes that gap. **PHP-only — nothing under `src/` or `assets/` changed, so no React/webpack rebuild.**
- **Transport.** MainWP Child's `extra_execution` callable runs `apply_filters( 'mainwp_child_extra_execution', $information, $post )` (with `$post = $_POST`) *after* its own RSA-signature verification, as the connected admin user, dispatched from `parse_init`/`init` priority 9999. Registering the bridge filter in `gswp_init()` on `plugins_loaded` priority 10 is safely earlier. Because the request is already authenticated and impersonating the admin, the bridge calls the REST **callbacks** directly and deliberately does **not** re-run `current_user_can()` — that is why the class does not touch the REST route or its `check_permissions()`.
- **Single-source validation.** `handle()` dispatches on `$post['mwpgswp_action']`: `get_settings` returns `(new GSWP_Rest_Api())->get_settings()->get_data()`; `update_settings` JSON-decodes `$post['settings']` (`wp_unslash`ed first — WordPress slashes superglobals), builds a `WP_REST_Request` for `POST /gswp/v1/settings`, copies each incoming key in with `set_param()`, and routes through `GSWP_Rest_Api::update_settings()`. So every rule stays in one place — threshold clamping, enum whitelists, alert-email validation, the alert-mode digest-cron reschedule, role validation against the child's real roles, and exempt-login resolution against the child's real users (which cannot be validated dashboard-side). Any future option wired into the REST route becomes MainWP-manageable automatically.
- **Wire-format contract (fixed — the extension is deployed).** Success (both actions) adds exactly one key: `$information['mwpgswp'] = array( 'success' => true, 'version' => GSWP_VERSION, 'woocommerce_active' => class_exists( 'WooCommerce' ), 'roles' => wp_roles()->get_names(), 'settings' => <full post-save settings map> )`. Failure (unknown action, undecodable payload) is `array( 'success' => false, 'error' => '…' )`. When `$_POST['mwpgswp_action']` is absent the filter returns `$information` **unchanged** — that absence of the `mwpgswp` key is how the dashboard tells "GSWP missing / older than 2.9.0" apart from an error, and it is what keeps the bridge invisible to every other extension that shares the `extra_execution` filter.
- **Inertness.** With MainWP Child absent, the class only registers a filter nobody fires: no output, no hooks on any core action, no front-end effect, and a normal MainWP sync passes through untouched (non-matching requests return `$information` as-is). Loaded unconditionally (required right after `class-gswp-rest-api.php`) because MainWP dispatches on the front end where `is_admin()` is false.
- **No changes** to `gswp_default_options()`, activation, migration, `GSWP_Rest_Api`, the REST route, options, or any existing class behaviour. Out of scope (mirrors the extension): no bulk/group apply, no 2FA user administration, no alert-log surfacing, no new REST routes — configuration transport only.
- Version bumped to 2.9.0 (main header, `GSWP_VERSION`, `readme.txt` stable tag + MainWP feature bullet + "Can I manage these settings from MainWP?" FAQ + changelog, `package.json`, `package-lock.json` root). `php -l` clean on both touched PHP files (`google-security-for-wordpress.php`, `includes/class-gswp-mainwp-child.php`). The new class file must ship in the distribution ZIP's explicit file list (the same list that excludes `tests/manual/` — see Phase 21); it is wired into the main file's `require_once` block so the plugin loads it.
- **Contract verified against the deployed extension.** Unzipped the actual shipped `mainwp-for-google-security-for-wordpress` v1.0.0 dashboard-extension ZIP and diffed `class/class-mwpgswp-connector.php` and `class/class-mwpgswp-settings-schema.php` against the companion repo clone used to write this bridge — both byte-identical. Cross-checked every settings key the dashboard schema defines against `GSWP_Rest_Api::get_settings()`/`update_settings()`: full 1:1 coverage, nothing the dashboard can send or expects back is unhandled by the bridge.
- **Test build packaged.** Since `build/` is gitignored and was never committed, ran `npm install && npm run build` (no `src/` changes this phase, so this just reproduces the 2.8.1-era admin screen under the new version) and assembled `google-security-for-wordpress-2.9.0.zip` from an explicit file list — `google-security-for-wordpress.php`, `readme.txt`, `includes/*.php` (17 files, `class-gswp-mainwp-child.php` included), `assets/`, `build/` — excluding `src/`, `tests/`, `node_modules/`, `.git/`, and the repo's planning docs. `php -l` clean on all 19 packaged PHP files. Delivered to the user for install-and-test on a staging child site; not committed to the repo (build artifact).
- See `PLAN-gswp-2.9.0-mainwp-bridge.md` (companion repo) for the full spec, wire-format contract, reference implementation, and the staging round-trip verification checklist.

## Historical Phase: Phase 22 (Tabbed settings screen + 2FA button text-colour fix)

### Phase 22 Modifications (v2.8.1)
- **Tabbed settings screen.** The Google Security settings page had grown to seven stacked full-width panels (~1,900 lines of JSX) rendered as one long scroll. Replaced the `grid grid-cols-1 … md:grid-cols-2` panel stack in `src/components/App.jsx` with a new hand-rolled `src/components/SettingsTabs.jsx` implementing the WAI-ARIA tabs pattern (`role="tablist"`/`"tab"`/`"tabpanel"`, arrow-key + Home/End navigation, roving `tabindex`, focus moves to the newly active tab). Chose hand-rolled tabs over `@wordpress/components` `TabPanel` to avoid pulling `wp-components` CSS into the scoped Tailwind wrapper.
- **Five tabs, grouped by task rather than one per component**: API Credentials (`SettingsPanel`), Form Protection (`PageToggles`), Enterprise Defense (`TransactionDefense` + `AccountDefender`), Two-Factor Auth (`TwoFactorNotice`), Alerts & Compatibility (`AlertSettings` + `Compatibility`). All seven panel components are unchanged internally (props identical); only their inert `md:col-span-2` root class was stripped since the grid it targeted no longer exists.
- **All tabs stay mounted** inside the single `<form onSubmit={handleSave}>` — inactive tabpanels get the `hidden` attribute, not conditional rendering. This keeps the one Save button submitting the complete settings object regardless of which tab is showing, avoids re-triggering `animate-fadeIn` reveals on every switch, and needs no new CSS (Tailwind v4 preflight already ships `[hidden]{display:none!important}`).
- **Active tab persisted in the URL hash** (`#tab=<id>`), read on mount via `getTabFromHash()` (validated against the tab list, falling back to `credentials`) and written with `history.replaceState` on change — deep-linkable, no scroll jump, no history spam, no router dependency.
- **2FA button text-colour bug, root cause and fix.** The "Set up Two-Factor Authentication" button was rendering WP-admin link-blue text despite the Phase 8 white-text override (`.gswp-admin-isolated a.gswp-2fa-btn` + `:hover/:focus/:active`). That override lived inside `@layer base` in `src/styles/index.css`. Tailwind v4 emits genuine CSS cascade layers, but WordPress admin's own stylesheets (`a{color:#2271b1}`, `a:hover{color:#135e96}`, etc.) are unlayered — and per the CSS cascade spec, unlayered author styles always beat layered ones regardless of specificity. So WP's plain `a` rule won over both the `text-white` utility and the override, a regression from the Tailwind v4 migration (this worked when Phase 8 shipped, before the project was on layered Tailwind output). Fix: moved the four selectors out of `@layer base` to plain top-level CSS (after the `@theme` block gets built), added `:visited` for completeness, and documented why the rule must stay unlayered. Verified in the built `build/index.css` that the rule now sits after the closing of `@layer utilities{...}`, outside every layer.
- No PHP, REST route, or settings-option changes — this is UI-only. Version bumped to 2.8.1 (main header, `GSWP_VERSION`, `readme.txt` stable tag + changelog, `package.json`, `package-lock.json` root) since no new options were added. React assets rebuilt (`npm run build`); `wp-scripts lint-js` and `lint-style` both clean.
- See `PLAN-settings-tabs-2fa-styling.md` for the full design write-up (tab grouping rationale, edge cases, verification checklist).

## Historical Phase: Phase 21 (Application-password hardening for 2FA-enforced roles)

### Phase 21 Modifications (v2.8.0)
- Closed the last password-only authentication path for accounts our 2FA policy protects. Role-based enforcement only guaranteed a second factor on **interactive** logins; a REST API or XML-RPC login with an **application password** bypassed the challenge by design (`GSWP_Two_Factor::enforce_second_factor()` waves REST requests through, and application-password auth never reaches an interactive challenge). A new opt-in toggle disables application passwords for users in a 2FA-enforced role via the core `wp_is_application_passwords_available_for_user` filter, so a single credential can no longer authenticate an enforced `manage_options` account. Off by default (agency management tooling commonly connects via an admin application password, so on-by-default would sever those connections on a routine update).
- **Bug fixed in the same phase:** `GSWP_Two_Factor::block_non_interactive()` — the XML-RPC block for enrolled users the readme/STATE.md have advertised since v2.0.0 — **had never been hooked** (added in the rebrand commit, no `add_filter` anywhere in history). An enrolled user's real password worked over XML-RPC with no second factor. It is now wired to `authenticate` at priority 99 (after the password/reCAPTCHA/Account Defender checks at 20/30/40, just before the interactive challenge at 100), restoring the documented behaviour. It stays scoped to **enrolled** users (enforcement locks the dashboard, not authentication, so an enforced-but-unenrolled user's XML-RPC password login is no stricter than their interactive one).
- **Mechanism.** New `restrict_app_passwords( $available, $user )` on `wp_is_application_passwords_available_for_user` (priority 10) returns `! role_is_enforced( $user->ID )` when the block is on — but only ever **narrows** an already-true `$available` (returns the incoming value, never a hard `true`), so it composes with other plugins that disable application passwords. Returning `false` closes every application-password surface at once: authentication with already-issued passwords (REST **and** XML-RPC, since core's `wp_authenticate_application_password()` handles both), the profile "Application Passwords" section, the REST management endpoints, and `authorize_application.php`. Existing passwords are **rejected, not revoked** (stored meta untouched), so turning the toggle off restores them instantly with no re-issuing. Two new static predicates mirror `remember_enabled()`: `app_password_block_enabled()` (master switch + `gswp_2fa_block_app_passwords`) and `is_app_password_exempt( $user )`.
- **Scope = enforced roles, not enrolled users** (an app password minted before enrolment would otherwise be a permanent bypass; and a user who *voluntarily* enrolled in an un-enforced role never has an integration break). **No grace-period interaction** — the block applies the moment the toggle is on (grace is a dashboard-UX affordance; app passwords aren't part of 2FA setup). The 2FA master switch off unregisters both new hooks, so it doubles as a kill switch.
- **Per-account exemption list** (`gswp_2fa_app_password_exempt_users`, default `''`), added for the agency's real tooling: **MCP-server connections authenticate with application passwords on admin accounts**. The exemption is surgical — it re-allows application passwords only; the account still gets the interactive 2FA challenge and every other control. Comma-separated logins, validated at save time in the REST route (`sanitize_user()` each, kept only when `get_user_by( 'login', … )` resolves, de-duplicated, re-joined) so typos are dropped rather than silently never matching; matched case-insensitively against `$user->user_login`. Standard MainWP Child connections use their own RSA-key handshake (not application passwords) and are unaffected — verified as part of rollout.
- Settings: new options `gswp_2fa_block_app_passwords` (`'0'`) and `gswp_2fa_app_password_exempt_users` (`''`), wired through `gswp_default_options()`, REST `get_settings`/`update_settings` (boolean toggle + the validate-and-rejoin exempt list), the admin localizer, `App.jsx` defaults, and a new toggle row + reveal-on-enable "Exempt accounts" text input inside the existing "Require for roles" section of `src/components/TwoFactorNotice.jsx` (with warning copy about the immediate effect on existing application passwords and the recommendation to use a dedicated service account). Updated the now-accurate comment in `enforce_second_factor()`.
- Version bumped to 2.8.0 (main header, `GSWP_VERSION`, `readme.txt` stable tag + feature bullet + FAQ + changelog including the XML-RPC fix, `package.json`, `package-lock.json` root). React assets rebuilt (`npm run build`); `php -l` clean on all touched PHP, `wp-scripts lint-js` clean on the JSX.
- **Verified on staging** (WordPress 6.4.3, via Novamira MCP PHP execution driven by a local LLM): all 13 checks passed — baseline auth, block-on refusal (`application_passwords_disabled_for_user`), exemption re-allow with non-exempt still refused, rejected-not-revoked reversibility, connecting MCP account unaffected, and the XML-RPC block (`gswp_2fa_required` for an enrolled user, non-enrolled untouched). The chunked test scripts are committed under `tests/manual/` (excluded from the distribution ZIP, which copies an explicit file list). Verification also surfaced a live site where the exemption option held an *array* (external tooling); `is_app_password_exempt()` now normalizes an array value by imploding it instead of the `(string)` cast that would warn and match nobody.
- Docs: corrected the stale checkout-block FAQ (block checkout protected since 2.5.0) and added a service-account setup FAQ (create → optionally enrol → **exempt before minting**, since the block hides the Application Passwords UI for non-exempt enforced accounts → mint named app password → enable policy with only service accounts exempt → cut over and revoke human app passwords → rotate/monitor). The FAQ is the seed for expanded standalone docs (planned follow-up).
- Out of scope (roadmap): per-user exemptions as scattered profile checkboxes (kept central in one settings screen instead); revoking/auditing existing application passwords (core's profile screen already lists per-user passwords); blocking XML-RPC entirely for enforced-but-unenrolled users (stricter than the interactive baseline); an email alert on a blocked application-password attempt (would ride the Phase 20 action-hook seam).

## Historical Phase: Phase 20 (Admin email alerts on flagged events)

### Phase 20 Modifications (v2.7.0)
- Turned the two highest-value security signals the plugin already detects into an email to the operator, instead of a log line nobody watches: **Account Defender flagging `SUSPICIOUS_LOGIN_ACTIVITY` on an administrator-capable account**, and **Transaction Defense blocking a high-risk checkout** (both the classic checkout and the WooCommerce Checkout block). Big operational win for an agency running many sites; the hard requirement was throttling so it never becomes spam.
- **Decoupled detection from delivery via internal action hooks.** The detection sites fire plain actions — `do_action( 'gswp_suspicious_admin_login', $user, $labels, $context )` and `do_action( 'gswp_checkout_blocked', $risk, $threshold, $context )` — and the new `GSWP_Alerts` is the only shipped subscriber, keeping mail logic out of the verifier/defender classes and leaving the actions as a seam for future Slack/webhook forwarding. The actions fire whenever the source feature detects the event (independent of whether alerts are enabled); `GSWP_Alerts` only hooks them when `gswp_alerts` is on.
- New `includes/class-gswp-alerts.php` (`GSWP_Alerts`, constructed in `gswp_init()`). Two-layer **throttle pipeline** (the core of the feature): (1) per-event dedupe — a transient `gswp_alert_dedupe_{md5(key)}` keyed `login_{user_id}` (6 h window) or `checkout_{md5(billing_email ?: ip)}` (1 h window) suppresses repeats of the same event; (2) a global circuit breaker — a rolling hourly counter transient caps immediate sends at 5 (filter `gswp_alert_hourly_cap`), and anything past the cap (or anything at all in a digest mode) is appended to a queue (`gswp_alert_queue`, autoload-off, capped at 100 with a dropped-count) that the digest cron flushes as **one** summary email. Delivery mode `gswp_alert_mode` ∈ `immediate` | `hourly` | `daily`; digest modes never mail immediately and an empty period sends nothing. Emails are **sent on `shutdown`** so a slow mailer never delays the login/checkout response; `wp_mail` failures are logged under verbose logging and otherwise ignored (fail-open). Recipients come from `gswp_alert_email` (comma-separated, each `sanitize_email`/`is_email`-validated) and fall back to `admin_email`. Plain-text emails; subject/body lead with the site name since an agency watches many sites.
- **Detection wiring.** `GSWP_Account_Defender::capture_login_assessment()` now, on any `SUSPICIOUS_LOGIN_ACTIVITY` label (independent of the `gswp_ad_step_up` branch), resolves the flagged account — `$user` when the password was correct, else `get_user_by( 'login'|'email', $username )` so a wrong-password credential-stuffing attempt still resolves — and fires `gswp_suspicious_admin_login` when it passes `user_can( $target, apply_filters( 'gswp_alert_login_capability', 'manage_options' ) )`. `GSWP_Verifier::process_fraud_prevention()` (classic) and `GSWP_Blocks::validate_store_checkout()` (Store API) fire `gswp_checkout_blocked` inside their existing `risk >= threshold` block branches, alongside the existing log line, passing billing/total context (from `$_POST`+cart classically, from the `$order` in the block path). No behaviour change to either block.
- **Cron lifecycle.** `GSWP_Alerts::maybe_schedule_digest()` (on `init`, always registered) reconciles the `gswp_alerts_digest_event` schedule to the mode: scheduled at the mode's recurrence for digest modes, scheduled hourly only while an overflow queue exists in immediate mode, and cleared when the feature is off or the mode's recurrence changes. The REST route clears the event on a mode change so the next load reschedules it; a new `gswp_deactivate()` (`register_deactivation_hook`, the plugin's first) clears it on deactivation.
- Settings: new options `gswp_alerts` (default `0`), `gswp_alert_email` (`''`), `gswp_alert_mode` (`immediate`), `gswp_alert_login`/`gswp_alert_checkout` (per-event sub-toggles, default `1`), wired through `gswp_default_options()`, REST `get_settings`/`update_settings` (toggles in the `$toggles` array; `alert_email` validated+re-joined; `alert_mode` whitelisted with cron-clear on change), and the admin localizer. New React panel `src/components/AlertSettings.jsx` (master toggle, recipients input, delivery select, two event sub-toggles with the checkout one gated on `woocommerceActive`, and the standard classic-key Enterprise notice) rendered in `App.jsx` with the five defaults added.
- Version bumped to 2.7.0 (main header, `GSWP_VERSION`, `readme.txt` stable tag + feature bullet + changelog, `package.json`, `package-lock.json` root). React assets rebuilt (`npm run build`); `php -l` clean on all touched PHP, `wp-scripts lint-js` clean on the JSX.
- Out of scope (roadmap): Slack/webhook delivery (the two actions are the seam); alerts for non-admin suspicious logins, `SUSPICIOUS_ACCOUNT_CREATION`, or account-modification events; a cross-site agency dashboard; HTML email templates.

## Historical Phase: Phase 19 (Account Defender step 3 — account-modification events)

### Phase 19 Modifications (v2.6.0)
- Completed the reCAPTCHA Enterprise **Account Defender** console checklist by extending it from the front door (login/registration) to **account-modification events**: password resets, email-address changes, and two-factor enable/disable now carry the same anonymous, salted `accountId` and are annotated `LEGITIMATE` when they complete, so Google's site-specific model learns what a real owner's in-account activity looks like — the takeover signal that was missing. **Account changes are only assessed and annotated, never blocked** (these are authenticated users; a low score must not lock someone out of their own profile).
- New sub-toggle `gswp_ad_events` (default `1`) under the Account Defender panel, gating a new `GSWP_Account_Defender::events_active()` (Enterprise + `gswp_account_defender` + `gswp_ad_events`). Enabling Account Defender therefore completes the checklist out of the box, but a site that objects to Google JS on `profile.php`/account pages can switch just the modification events off without losing login coverage. Wired through `gswp_default_options()`, REST `get_settings`/`update_settings`, the admin localizer, `App.jsx` default, and a third toggle row in `src/components/AccountDefender.jsx` ("Assess account changes").
- `GSWP_Verifier`: extended the `build_account_user_info()` context whitelist with `wp_lostpassword`, `password_reset`, and `account_update` so `event.userInfo.accountId` is attached on those assessments too (login/registration unchanged). No other verifier change — `verify_token()` already accepts the identifier and captures the assessment name/labels.
- `GSWP_Account_Defender` (new "account-modification events" section, registered only when `events_active()`): injects the token field into the **own-profile** form (`show_user_profile`, priority 1) and loads the reCAPTCHA script on `profile.php` **only** (`admin_enqueue_scripts` — never `user-edit.php`, so an admin editing another user is never attributed to that account); assesses a sensitive self-profile save on `user_profile_update_errors` (email/password/2FA change detected) and annotates the committed outcome on `profile_update`; injects + assesses + annotates the **WooCommerce "Account details"** form (`woocommerce_edit_account_form` / `woocommerce_save_account_details_errors` / `woocommerce_save_account_details`). A single reCAPTCHA action (`account_update`) per form (tokens are single-use), with the terminal hooks deciding the annotation; a shared request-scoped guard (`$modification_annotated`) makes the profile_update annotation (fired inside `wp_update_user`) and the WooCommerce annotation idempotent. Added a small self-expiring **pending-assessment store** (`store_pending()`/`take_pending()`, transients) for the two deferred flows — the profile email-change confirmation link and the password-reset email link — which complete in a later request with no token; they're annotated once email control is proven.
- `GSWP_Login` owns the **password-reset lifecycle** (it already owns the wp-login.php forms): `validate_lostpassword()` now takes the requested `WP_User` (2nd arg of `lostpassword_post`), attaches the accountId, and stashes the assessment as pending `lostpw_{ID}`; new `resetpass_form` field injection (action `password_reset`, added to the `#resetpassform` selector in the login bootstrap JS), `validate_password_reset` assessment (guarded to the actual submission, not the GET render, and never blocking), and `after_password_reset` annotation of both the reset-form assessment and the stashed lost-password assessment. Script printing now also fires when only the reset path is active. WooCommerce resets that bypass `validate_password_reset` still get the lost-password pending annotated via core `after_password_reset`.
- `GSWP_Two_Factor::save_profile()`: on a successful self-service enable/disable it calls `GSWP_Account_Defender::note_2fa_change('enabled'|'disabled')` instead of annotating inline. **Implementation note / deviation from the plan:** `personal_options_update` (where `save_profile` runs) fires *before* `edit_user()` creates the `account_update` assessment (`user_profile_update_errors`), so inline annotation there would have no assessment yet; the flag is read at `profile_update` and enrolment adds the `PASSED_TWO_FACTOR` reason. Admin-initiated 2FA resets for another user are never annotated (not self-service).
- Version bumped to 2.6.0 (main header, `GSWP_VERSION`, `readme.txt` stable tag + feature bullet + changelog, `package.json`, `package-lock.json` root). React assets rebuilt (`npm run build`); `php -l` and `wp-scripts lint-js` clean.
- Out of scope (roadmap): **step 4 — site-wide passive telemetry script** (loading reCAPTCHA on every front-end page for continuous behavioural signal), to ship as its own opt-in, default-off, per-client toggle since it trades page weight for signal.

## Historical Phase: Phase 18 (WooCommerce Blocks / Store API checkout support)

### Phase 18 Modifications (v2.5.0)
- Closed the biggest coverage gap: the modern **WooCommerce Checkout block** submits over the **Store API**, so none of the classic-checkout hooks fired — a store using the block silently got **zero reCAPTCHA scoring and zero Transaction Defense** (the Mad Dog near-miss). The block is now protected by the same scoring, thresholds, and Enterprise Transaction Defense as the classic shortcode checkout, with no new settings (`gswp_enable_checkout` and the existing thresholds apply to both).
- New `includes/class-gswp-blocks.php` (`GSWP_Blocks`, constructed in `gswp_init()` with the shared verifier). Registers the JS integration on `woocommerce_blocks_checkout_block_registration`, declares the `gswp` checkout extension schema via `woocommerce_store_api_register_endpoint_data()` (`CheckoutSchema::IDENTIFIER`, falling back to the literal `'checkout'`) on `woocommerce_blocks_loaded`, and scores the token on `woocommerce_store_api_checkout_update_order_from_request`. It reads the token from `$request['extensions']['gswp']['token']`, calls `GSWP_Verifier::assess_checkout_token()`, and on failure throws a `RouteException` (HTTP 400) so the block surfaces the message — the Store API equivalent of a checkout validation error. On success it writes `_gswp_assessment_name` (the `GSWP_Transaction_Defense::META_ASSESSMENT` constant) and a risk order note directly on the order (no WC-session bounce needed), and enforces the opt-in high-risk block (`gswp_txn_block` / `gswp_threshold_txn`). Inert unless WooCommerce Blocks is present.
- New `includes/class-gswp-blocks-integration.php` (`GSWP_Blocks_Integration implements Automattic\WooCommerce\Blocks\Integrations\IntegrationInterface`), required lazily from the registration hook so the plugin stays parseable without Blocks. `initialize()` registers the Google reCAPTCHA loader (`enterprise.js`/`api.js`) and the block script with dependency handles `wp-element`, `wp-plugins`, `wp-data`, `wp-i18n`, `wc-blocks-checkout`, `google-recaptcha-v3`, and localizes `gswpBlocksData` (siteKey, isEnterprise, enabled).
- New hand-written asset `assets/js/gswp-blocks-checkout.js` (no build step, mirroring `gswp-2fa-modal.js`; reads runtime globals). Registers an invisible `ExperimentalOrderMeta` fill via `registerPlugin` (scope `woocommerce-checkout`); inside the fill it keeps a fresh reCAPTCHA token in the checkout extension data (`setExtensionData('gswp','token',…)`), priming on mount, refreshing on a 90s interval, and refreshing on the rising/falling edges of the checkout store's processing state (single-use tokens need a fresh one for retries). The **server enforces the policy** — a missing/low token blocks the checkout — so if reCAPTCHA fails to load the classic-parity missing-token block still applies.
- `GSWP_Verifier` refactor (additive, backward-compatible): `verify_token()` gained an optional 5th `$token` param (defaults to reading the posted `g-recaptcha-response` field, so every existing caller is unchanged); extracted a shared `format_address()` used by both the posted-field and order-based builders; added `build_checkout_event_extra_from_order()`, `build_transaction_user_from_order()`, `build_transaction_items_from_order()` (build Enterprise `transactionData` from a `WC_Order` since the Store API carries no `$_POST`); added the public `assess_checkout_token( $token, $order )` and a `get_last_fraud_risk()` getter. `GSWP_Transaction_Defense` is unchanged — its `woocommerce_order_status_changed` annotation already fires for block orders once the assessment meta is present.
- Bumped version to 2.5.0 (main header, `GSWP_VERSION`, `readme.txt` stable tag + features + changelog, `package.json`, `package-lock.json` root). The block script ships as a plain committed asset, so no webpack rebuild is required for this feature.

## Historical Phase: Phase 17 (2FA enforcement grace period + settings background fix)

### Phase 17 Modifications (v2.4.0)
- Added a configurable **grace period** to role-based 2FA enforcement so client rollouts can enable enforcement without instantly locking users out. New option `gswp_2fa_grace_days` (default `14`, clamped 0–30 by REST; `0` = the old immediate behaviour).
- `GSWP_Two_Factor::maybe_enforce_setup()` now consults `grace_deadline( $user_id )` before redirecting: within the window it registers an `admin_notices` countdown (`render_grace_notice()` — days remaining, localized deadline date via `date_i18n`, and a "Set up now" button to `profile.php#gswp-2fa`) and lets the dashboard work normally; past the deadline the existing profile lock/redirect applies.
- The per-user clock starts on the user's **first admin visit while enforcement applies** (`gswp_2fa_grace_start` user meta, set lazily in `grace_deadline()`), so users hired or role-enforced later get the same full window. The meta is cleared on successful enrolment (`save_profile`) and by `disable_for_user()` so an admin 2FA reset (lost device) grants a fresh window instead of an instant lockout.
- Settings wiring: `gswp_default_options()`, REST `get_settings`/`update_settings` (`tfa_grace_days`, integer-clamped 0–30), admin localizer, `App.jsx` default, and a "Grace period (days)" number input inside the "Require for roles" section of `src/components/TwoFactorNotice.jsx`.
- UI fix: removed the `bg-gray-50/50 min-h-screen` tint from the App root (`src/components/App.jsx`) so the settings screen sits on the standard WP admin background instead of a mismatched backdrop.
- Bumped version to 2.4.0 (main header, `GSWP_VERSION`, `readme.txt` stable tag + changelog, `package.json`, `package-lock.json` root). Assets rebuilt.

## Historical Phase: Phase 16 (2FA "remember this browser" trusted devices)

### Phase 16 Modifications (v2.3.0)
- Added a "Remember this browser for 30 days" trusted-device option to the 2FA challenge so enrolled users (e.g. mandatory manage_options accounts) aren't prompted for a code every login.
- Flow: the code-entry modal (`assets/js/gswp-2fa-modal.js`) shows a "Remember this browser for 30 days" checkbox when the server localizes `rememberEnabled`; its state posts as `remember_device` to `ajax_verify()`. On a successful verification, `GSWP_Two_Factor::remember_device()` generates a 64-char random token, stores its `hash_hmac('sha256', token, wp_salt('auth'))` in the `gswp_2fa_trusted_devices` user meta (with expiry/created/UA-hash, pruned + capped at 10), and sets the `gswp_2fa_trusted` cookie = "user_id|token" (HTTP-only, Secure, SameSite=Lax, 30d, `cookie_path()`).
- `enforce_second_factor()` now skips the challenge when `is_trusted_device($user->ID)` matches a stored unexpired hash for that same user — bound per-user so the cookie can't waive 2FA for a different account — **unless** `step_up_forced()` is true (Account Defender flagged `SUSPICIOUS_LOGIN_ACTIVITY`), in which case the code is always required.
- Revocation: `disable_for_user()` and a new profile "Forget all remembered browsers" checkbox clear the meta + cookie; `after_password_reset` calls `forget_all_devices()`. The manage screen shows a live remembered-browser count. Security posture: tokens are high-entropy, only the HMAC is stored (cookie value never derivable from the DB), `hash_equals` comparison, HTTP-only/Secure/SameSite cookie; the inherent trade-off (cookie theft = 2FA bypass within the window) is mitigated the same way major providers do.
- New setting `gswp_2fa_remember` (default `1`), exposed via `remember_enabled()`, wired through `gswp_default_options()`, REST `get_settings`/`update_settings`, the admin localizer, `App.jsx` default, and a new "Allow 'Remember this browser'" toggle in `src/components/TwoFactorNotice.jsx`. Modal CSS gained a `.gswp-2fa-remember` row. Assets rebuilt.
- Bumped version to 2.3.0 (main header, `GSWP_VERSION`, `readme.txt` stable tag + changelog, `package.json`, `package-lock.json` root).

## Historical Phase: Phase 15 (Requirements verification, Git clean-up, & formatting checks)

### Phase 15 Modifications (v2.2.1-patched)
- Added active runtime verification checks for WordPress 5.8+ and PHP 7.4+ inside [google-security-for-wordpress.php](file:///Users/rwaterbury/Developer/google-security-for-wordpress/google-security-for-wordpress.php) to gracefully deactivate the plugin and show a dashboard notice if minimum requirements are not satisfied, preventing parse/fatal errors on unsupported environments.
- Ran the autoformatter (`wp-scripts lint-js --fix`) to standardize spacing and formatting in [src/components/PageToggles.jsx](file:///Users/rwaterbury/Developer/google-security-for-wordpress/src/components/PageToggles.jsx), [src/components/SettingsPanel.jsx](file:///Users/rwaterbury/Developer/google-security-for-wordpress/src/components/SettingsPanel.jsx), and [src/components/TwoFactorNotice.jsx](file:///Users/rwaterbury/Developer/google-security-for-wordpress/src/components/TwoFactorNotice.jsx) to match WordPress coding style guidelines.
- Rewrote the Git history to strip `Co-Authored-By` and `Claude-Session` metadata lines from all commit messages, and force-pushed the clean history to the remote repository.
- Removed remote branches (`claude/*`), leaving only a single clean `main` branch.
- Rebuilt JS/CSS assets and packaged the final ZIP distribution.

## Historical Phase: Phase 14 (Quieter logging + verbose toggle)

### Phase 13 Modifications (v2.2.0)
- Added reCAPTCHA Enterprise **Account Defender** for login/registration. Disabled by default, Enterprise-only. Three pieces: send an anonymous account identifier, interpret the returned labels (log + optional 2FA step-up), and annotate login/2FA outcomes to train Google's site-specific model.
- `GSWP_Verifier`: `verify_token()` gained a 4th `$account_identifier` arg. `build_account_user_info()` attaches `event.userInfo { accountId, createAccountTime }` on login/registration assessments only when Account Defender is on. `resolve_account_id()` keys existing users by their immutable user ID (stable across username/email logins) and registrations by normalised email; `hash_account()` SHA-256s with a lazily-seeded, stored `gswp_account_salt` so only an opaque hash — never email/username/phone — leaves the site. The assessment captures `accountDefenderAssessment` alongside the name; new getters `get_last_assessment_name()` / `get_last_account_labels()`. Identifiers are threaded from every login/registration entry point: WooCommerce (`validate_login` username, `validate_registration` email), WP core (`GSWP_Login` — the resolved `WP_User` on a correct password, else the `log` field; registration email), Xootix (`$creds['user_login']` / email), and PowerPack (`$user_login`).
- New `includes/class-gswp-account-defender.php` (`GSWP_Account_Defender`), constructed with the shared verifier and inert unless `is_active()` (Enterprise + `gswp_account_defender`). Hooks `authenticate` at priority 40 (between the verifier's scoring at 30 and 2FA enforcement at 100) to capture the assessment name + labels into request statics, log any labels, and set a `force_2fa` flag when `gswp_ad_step_up` is on and `SUSPICIOUS_LOGIN_ACTIVITY` is present. Annotates terminal login outcomes via the global `wp_login` (→ `LEGITIMATE` + `CORRECT_PASSWORD`) and `wp_login_failed` (→ `INCORRECT_PASSWORD`, skipping our own `gswp_2fa_required` hold). `annotate()` POSTs to the Enterprise `{name}:annotate` endpoint, fail-open with logging (WC logger, or error_log under WP_DEBUG on non-Woo sites).
- 2FA wiring (`GSWP_Two_Factor`): `enforce_second_factor()` now annotates `CORRECT_PASSWORD` + `INITIATED_TWO_FACTOR` when arming a challenge and persists the assessment name into the pending-login transient (`store_pending_login()` gained an `$assessment_name` param). `ajax_verify()` annotates `LEGITIMATE` + `PASSED_TWO_FACTOR` on success and `FAILED_TWO_FACTOR` on a wrong code, via a small `annotate_2fa()` guard. Held 2FA logins never reach `wp_login` (completion uses `wp_set_auth_cookie`), so there is no double annotation. Step-up forces the challenge for enrolled users only; non-enrolled flagged users are logged, never blocked (manage_options accounts are enrolled by policy).
- New options `gswp_account_defender`, `gswp_ad_step_up` (both default `0`) and lazily-seeded `gswp_account_salt`, wired through `gswp_default_options()`, REST `get_settings`/`update_settings`, and the admin localizer. New React panel `src/components/AccountDefender.jsx` (Enterprise-only; enable toggle + "Require 2FA on suspicious logins" toggle) rendered in `App.jsx`. Assets rebuilt.
- Bumped version to 2.2.0 (main header, `GSWP_VERSION`, `readme.txt` stable tag + features + changelog, `package.json`, `package-lock.json` root).

## Historical Phase: Phase 12 (reCAPTCHA Enterprise Transaction defense)

### Phase 12 Modifications (v2.1.0)
- Added reCAPTCHA Enterprise **Transaction defense** for the WooCommerce checkout. Turning on the console toggle alone produced nothing because the assessment request carried no transaction data; the plugin now sends `event.transactionData` so Google returns a `fraudPreventionAssessment`. Disabled by default and Enterprise-only (classic v3 has no fraud prevention).
- `GSWP_Verifier`: `validate_checkout()` now builds extra event fields via `build_checkout_event_extra()` and passes them through `verify_token()` → `assess_enterprise_token()` (which now `array_merge`s the extra fields into `event` and sets `fraudPrevention => "ENABLED"`). Transaction data is assembled from the posted checkout fields + `WC()->cart`: billing/shipping `Address` (recipient, lines, locality, administrativeArea, regionCode, postalCode), `paymentMethod`, `currencyCode`, `value`, `shippingValue`, payer `user` (email, plus accountId/creationMs/emailVerified for logged-in customers), and cart `items` (name, per-item value, quantity). Transaction data is only attached when Google's documented minimum is present (billing regionCode + postalCode + paymentMethod); otherwise it is omitted so the request can't 400 and lose the reCAPTCHA score check. The assessment captures `name` and `fraudPreventionAssessment` into instance properties.
- `process_fraud_prevention()` logs the `transactionRisk`, stashes the assessment name + risk in the WooCommerce session (the order does not exist yet at `woocommerce_after_checkout_validation`), and — when the opt-in `gswp_txn_block` setting is on — blocks the checkout when `transactionRisk >= gswp_threshold_txn` (default 0.8). The existing reCAPTCHA score-threshold block is unchanged and always applies.
- New `includes/class-gswp-transaction-defense.php` (`GSWP_Transaction_Defense`) completes the annotation feedback loop. Inert unless WooCommerce is active, the key type is Enterprise, and `gswp_txn_defense` is on. On `woocommerce_checkout_order_processed` it moves the assessment name from the session to `_gswp_assessment_name` order meta (and adds a risk order note); on `woocommerce_order_status_changed` it calls the Enterprise `assessments.annotate` endpoint — completed/processing → `LEGITIMATE` (+ `PAYMENT_GIVEN`), refunded → `FRAUDULENT` (+ `REFUND`), cancelled/failed → `FRAUDULENT`. Annotation is fail-open (logs and ignores errors) and guarded against re-firing via `_gswp_annotated` meta.
- New settings `gswp_txn_defense`, `gswp_txn_block` (both default `0`) and `gswp_threshold_txn` (default `0.8`), wired through `gswp_default_options()`, the REST `get_settings`/`update_settings` route (toggles + 0–1 clamped threshold), and the admin localizer. New React panel `src/components/TransactionDefense.jsx` (rendered in `App.jsx` only when WooCommerce is active; shows an Enterprise-required notice for classic keys) provides the enable toggle, the high-risk block toggle, and the risk-threshold slider. Assets rebuilt with `npm run build`.
- Bumped version to 2.1.0 (main file header, `GSWP_VERSION`, `readme.txt` stable tag + features + changelog, `package.json`, `package-lock.json` root entries).

## Historical Phase: Phase 11 (2FA enrolment: inline submit button + scroll back to section)

### Phase 11 Modifications (v2.0.2)
- Added an "Enable two-factor authentication" submit button directly beneath the verification-code field in the first-time enrolment UI (`GSWP_Two_Factor::render_setup_rows()`, via `submit_button( …, 'primary', 'gswp_2fa_setup_submit', false )`). It lives inside the profile `<form>`, so it submits to the existing `save_profile()` handler — no new server logic. Previously the only submit control was the core "Update Profile" button at the very bottom of the long profile page.
- After a setup submit, the page now returns to the authenticator section instead of the top of the page, so the one-time backup codes are immediately visible to copy. Implemented client-side: the backup-codes notice and the wrong-code error notice in `maybe_render_notices()` now carry a `gswp-2fa-scroll-target` class; the QR inline script (`get_qr_inline_js()`) gains a `scrollToSection()` that, on DOM ready, scrolls `#gswp-2fa` into view when that marker is present. A redirect-fragment approach was rejected because the profile POST→redirect→GET drops the URL fragment.
- Bumped version to 2.0.2 (main file header, `GSWP_VERSION`, `readme.txt` stable tag + changelog, `package.json`, `package-lock.json`). Profile assets are plain files served from `assets/` (QR inline JS is generated in PHP), so no webpack rebuild was required.

## Historical Phase: Phase 10 (Fix 2FA popup not opening on cached AJAX-login sites)

### Phase 10 Modifications (v2.0.1)
- Fixed the 2FA code-entry popup failing to open on the live site (`maddogproducts.com`) while working on staging (`maddog.onedog.solutions`). Symptom: an enrolled user's login via the Login/Signup Popup (Xootix) AJAX form was correctly held server-side (our `WP_Error` text appeared inside the Xootix popup), but the authenticator modal never rendered (no `<div>`, no console error), locking the user out.
- Root cause: `assets/js/gswp-2fa-modal.js` only opened the modal on a full-page reload (flag cookie already present) or after catching a native `submit` event on a password form, after which it polled the flag cookie for ~10s. The live site runs **FlyingPress** (page cache + "Delay JavaScript"); with the modal script deferred until user interaction and the Xootix login submitting over AJAX, the `submit`-triggered poll never started, so the held-login flag cookie was never observed. Staging has no such optimiser, so the timing happened to work.
- Fix: replaced the `submit`-event-gated poll with a **life-of-page poll of the flag cookie** (`startPolling()`, every 500ms), plus the existing on-load check and a new `pageshow` (bfcache) check. The modal now opens the moment the server's held-login flag cookie appears — independent of which login form/plugin was used, any page cache, or deferred-script timing. `open()` is now idempotent (`isOpen` guard) so repeated polls don't re-focus or rebuild the modal.
- Bumped version to 2.0.1 (main file header, `GSWP_VERSION`, `readme.txt`, `package.json`) so the asset's `GSWP_VERSION` cache-busting query arg forces FlyingPress/CDN/browser caches to fetch the new script. The popup assets are plain files served from `assets/` (not webpack-built), so no rebuild was required.

## Historical Phase: Phase 9 (2FA code-entry popup; settings consolidated into Google Security page)

### Phase 9 Modifications
- Replaced the redirect-based 2FA challenge (which caused an `ERR_TOO_MANY_REDIRECTS` loop and errored on My Account) with a **popup/modal flow that never redirects**. `GSWP_Two_Factor::enforce_second_factor()` hooks `authenticate` at priority 100: for an enrolled user it arms a single-use pending login (HTTP-only token cookie + transient + readable flag cookie) and returns a `WP_Error`, blocking the sign-in uniformly across wp-login.php, WooCommerce My Account, and the PowerPack AJAX login — no auth cookie is issued without the second factor, and nothing is rendered/redirected so front-end forms can't fatal.
- Added a plain-JS popup (`assets/js/gswp-2fa-modal.js` + `assets/css/gswp-2fa-modal.css`, enqueued on wp-login.php via `login_enqueue_scripts` and on logged-out front-end pages via `wp_enqueue_scripts`). It opens automatically when the readable flag cookie is present (full-page-reload logins) or shortly after a password form is submitted (AJAX logins that never reload), collects the code, and completes the login by posting to the `gswp_2fa_verify` admin-ajax endpoint, then navigates to the returned redirect.
- `ajax_verify()` is authorised by the unguessable HTTP-only pending token (not a WP nonce — a page cache such as FlyingPress would serve a stale nonce on logged-out pages). The pending cookies are set `SameSite=Lax` (the CSRF defence), and failed guesses are capped at 5 before the held login is discarded. Replay is still blocked via the last-used TOTP time step.
- Removed the redirect/interstitial machinery (`maybe_start_challenge`, `handle_challenge`, `maybe_resume_challenge`, `show_challenge`, `challenge_url`) and the `login_form_gswp_2fa` / `wp_login` / `template_redirect` hooks.
- Consolidated the 2FA settings into the Google Security React screen and removed the standalone Settings → Two-Factor Auth submenu (deleted `includes/class-gswp-two-factor-admin.php`). The master switch (`tfa_enabled`) and per-role enforcement (`tfa_enforced_roles`) are now read/written through the existing REST settings route (`class-gswp-rest-api.php`) and rendered inside `src/components/TwoFactorNotice.jsx` (toggle + role checkboxes), with the role list passed via the admin localizer (`roles`).

## Historical Phase: Phase 8 (Fix 2FA login flow across all entry points + admin UI tweaks)

### Phase 8 Modifications
- Fixed a fatal error (HTTP 500) when an enrolled user logged in from a non-`wp-login.php` form (e.g. the WooCommerce "My Account" page). `GSWP_Two_Factor::maybe_start_challenge()` previously rendered the interstitial inline by calling `login_header()`/`login_footer()`, which only exist inside `wp-login.php`; on a front-end login they are undefined and fatal. The challenge is now always rendered on `wp-login.php`: `maybe_start_challenge()` clears the freshly issued auth cookie, stashes the in-progress login behind a single-use, HTTP-only cookie token (`gswp_2fa_pending`) backed by a 5-minute transient, and `wp_safe_redirect()`s to `wp-login.php?action=gswp_2fa`.
- Closed a 2FA bypass on AJAX logins (the PowerPack Login Form module). `maybe_start_challenge()` used to `return` early on `wp_doing_ajax()`, leaving the auth cookie in place so the user was logged in with no second factor. It now clears the auth cookie and sets the pending cookie for AJAX logins too; a new `maybe_resume_challenge()` guard (hooked to both `login_init` and `template_redirect`) detects a logged-out visitor carrying a valid pending cookie and redirects them to the interstitial, so the half-finished AJAX login is forced through 2FA on the next page load.
- Replaced the per-user login-nonce mechanism (`create_login_nonce()`/`verify_login_nonce()`, stored in user meta) with the cookie-token + transient "pending login" helpers (`store_pending_login()`, `get_pending_login()`, `clear_pending_login()`, `challenge_url()`, `cookie_path()`). The token is unguessable and HTTP-only, so it can only be minted by a successful password check, and the TOTP code is still required. `show_challenge()` no longer emits hidden `user_id`/`nonce`/`redirect`/`rememberme` fields (the cookie carries that state).
- The post-2FA redirect now honours both `redirect_to` (wp-login.php) and `redirect` (WooCommerce My Account) request fields.
- Admin settings screen (React): `TwoFactorNotice` now shows a "Two-Factor Authentication Settings" button linking to Settings → Two-Factor Auth (new `twoFactorSettingsUrl` localized from `GSWP_Admin`), placed above the "Set up 2FA from your user profile" link. Both button-styled links carry a `gswp-2fa-btn` class with a scoped CSS rule in `src/styles/index.css` forcing white text (including `:hover`/`:focus`), since WordPress admin's own `a` colour rules otherwise won over Tailwind's `text-white`.

## Historical Phase: Phase 7 (Remove Key Scavenger + 2FA Profile Notice + Legacy Plugin Takeover)

### Phase 7 Modifications
- Removed the Smart Key Scavenger entirely: deleted `includes/class-gswp-key-scavenger.php` and `src/components/KeyScavenger.jsx`, dropped the `POST /gswp/v1/scan-keys` REST route and its `scavenge_keys()` callback, and removed the scavenger require/import wiring from the main file and `App.jsx`.
- Added `src/components/TwoFactorNotice.jsx`: a dialogue/notice panel on the settings screen explaining that 2FA enrolment happens in the user Profile, with a button linking to `profile.php#gswp-2fa`. The admin localizer now passes a `profileUrl` to the React app.
- Renamed the settings menu/page from "reCAPTCHA v3" to "Google Security" (`includes/class-gswp-admin.php`) and updated the React header accordingly.
- On activation (`gswp_activate`), the plugin now imports the predecessor "Google reCAPTCHA v3 for WooCommerce" plugin's `recaptcha_woo_*` options into the `gswp_*` keys via the shared `gswp_import_legacy_options()`, then deactivates and deletes that old plugin (matched by basename `google-recaptcha-v3-for-woocommerce/...`, text domain, or plugin name) with `deactivate_plugins()` + `delete_plugins()`. `gswp_maybe_migrate()` still imports options on every load as an upgrade safety net (without touching plugin files).

## Historical Phase: Phase 6 (Rebrand to Google Security for WordPress + Two-Factor Authentication)

### Phase 6 Modifications (v2.0.0)
- Renamed the plugin from "Google reCAPTCHA v3 for WooCommerce" to "Google Security for WordPress". Renamed the main file to `google-security-for-wordpress.php` and all `includes/class-recaptcha-woo-*.php` files to `includes/class-gswp-*.php`.
- Renamed all internal identifiers: text domain/slug -> `google-security-for-wordpress`; constants `RECAPTCHA_WOO_*` -> `GSWP_*`; class prefix `Recaptcha_Woo_*` -> `GSWP_*`; option/function prefix `recaptcha_woo_*` -> `gswp_*`; REST namespace `recaptcha-woo/v1` -> `gswp/v1`; JS globals/handles/IDs accordingly. Google's own field names (`g-recaptcha-response`, `data-recaptcha-action`, the `google-recaptcha-v3` script handle) were left untouched.
- Added `gswp_maybe_migrate()` (on `plugins_loaded`): one-time copy of legacy `recaptcha_woo_*` options to the new `gswp_*` keys so existing installs keep their settings. Old options are left in place as a safety net.
- Added `includes/class-gswp-totp.php`: self-contained RFC 6238 TOTP / RFC 4226 HOTP engine with Base32 (RFC 4648) secrets. Verified against the official RFC 6238 SHA-1 test vectors.
- Added `includes/class-gswp-two-factor.php`: per-user enrolment on the profile screen (QR via in-browser `qrcode-generator` with a manual setup-key fallback, filterable via `gswp_2fa_qr_script_url`), an interstitial login challenge after the password check (`wp_login` + `login_form_gswp_2fa`, mirroring the Two-Factor feature plugin), single-use hashed backup codes, replay protection via the last-used time step, admin reset of another user's 2FA, optional role-based enforcement, and an XML-RPC block for enrolled accounts.
- Added `includes/class-gswp-two-factor-admin.php`: server-rendered Settings -> Two-Factor Auth page (master toggle + per-role enforcement) using the Settings API (no build step required).
- Bumped version to 2.0.0 across the main file header, `GSWP_VERSION`, `readme.txt`, and `package.json`; refreshed the readme description, features, FAQ, and changelog.

## Current Phase (historical): Phase 5 (Project Finalization & Artifact Bundling)

### Modifications Done
- Created `.gitignore` to prevent tracking of development artifacts (like `node_modules` and compiled zips).
- Updated `package.json` to pin stable releases of Tailwind CSS (`^4.0.0`) and `@tailwindcss/postcss` (`^4.0.0`), and added linting scripts.
- Added `webpack.config.js` extending WordPress's default scripts configuration to bundle JavaScript and extract CSS correctly.
- Created `google-security-for-wordpress.php` (main bootstrap file).
- Created `includes/class-gswp-key-scavenger.php` to scan and scavenge keys from Fluent Forms, Gravity Forms, Beaver Builder options, and Beaver Builder postmeta.
- Created `includes/class-gswp-admin.php` to register the submenu under WooCommerce settings and enqueue React entrypoints using localizing scripts.
- Created `includes/class-gswp-rest-api.php` exposing `GET /settings`, `POST /settings` (with page-specific score thresholds), and `GET /scavenge`.
- Created React components `src/components/App.jsx`, `src/components/SettingsPanel.jsx`, `src/components/PageToggles.jsx`, `src/components/KeyScavenger.jsx`, `src/components/StatusBadge.jsx`, and Webpack entrypoint `src/index.js`.
- Created `src/styles/index.css` defining the scoped Tailwind v4 styling.
- Installed node module dependencies (`npm install`).
- Compiled assets with webpack successfully (`npm run build`).
- Created `includes/class-gswp-frontend.php` to inject the Google API scripts and hidden inputs onto WooCommerce login, registration, and checkout screens.
- Created `includes/class-gswp-verifier.php` to validate incoming reCAPTCHA tokens server-side using Google API and WP_Error handling.
- Created standard plugin meta file `readme.txt`.
- Optimized the database key scavenger flow: replaced the automatic, on-load database scanning query with an explicit user-triggered manual scan (`POST /gswp/v1/scan-keys`).
- Added check for `fluentform_settings` option within the key scavenger logic.
- Introduced interactive frontend states (`isScanning`, `scanPerformed`, `discoveredData`) with user controls styled using Tailwind v4.
- Reworked frontend token handling (v1.1.0): tokens are pre-fetched on page load and refreshed before the two-minute expiry, on `updated_checkout`, on `checkout_error`, and on tab refocus, so gateway-driven submissions (Stripe UPE, PayPal PPCP) always carry a valid token. Inline JS now attaches via `wp_add_inline_script` so it is never duplicated inside AJAX checkout fragments. The bootstrap is dependency-free vanilla JS (no jQuery): fragment replacements and checkout error notices are detected with a MutationObserver, and WooCommerce's jQuery checkout events (`updated_checkout`, `checkout_error`, `checkout_place_order` veto) are bound only as a progressive enhancement when jQuery is present.
- Added reCAPTCHA Enterprise support (v1.1.0): new `key_type`, `gcp_project_id`, and `gcp_api_key` settings; frontend loads `enterprise.js` and uses `grecaptcha.enterprise`; verifier creates assessments via `recaptchaenterprise.googleapis.com` with expected-action checking.
- Hardened verification failure modes (v1.1.0): credential misconfiguration (invalid secret, bad API key/project) logs a warning to the WooCommerce logger and fails open instead of blocking customers; expired/duplicate tokens return a dedicated "verification expired" message.
- Added WordPress core screen support (v1.3.0): new `includes/class-gswp-login.php` scores the wp-login.php sign in (`authenticate` filter), user registration (`registration_errors` filter), and lost password (`lostpassword_post` action) forms, each with its own threshold. Scripts and the token bootstrap are emitted directly in `login_footer` since wp-login.php skips the standard enqueue pipeline.
- Made WooCommerce optional (v1.3.0): removed the `Requires Plugins: woocommerce` header, moved the settings page from the WooCommerce submenu to Settings -> reCAPTCHA v3 (`add_options_page`), switched the capability/REST permission from `manage_woocommerce` to `manage_options`, and gated the WooCommerce form toggles in the admin UI behind a `woocommerceActive` flag.
- Added shared `includes/class-gswp-assets.php` (v1.3.0): centralizes the Google API script enqueue (classic/enterprise) and a reusable, vanilla-JS token refresh bootstrap that keeps every `.g-recaptcha-response` field populated (on load, before expiry, on refocus, and via MutationObserver for dynamically added fields).
- Added Login/Signup Popup integration (v1.3.0): `includes/class-gswp-xootix.php` injects the token field via the plugin's `xoo_el_form_end` template action and validates through its `xoo_el_process_login_errors` / `xoo_el_process_registration_errors` / `xoo_el_process_lostpw_errors` filters, reusing the WordPress core toggles, thresholds, and verifier. Inert unless `easy-login-woocommerce` is active.
- Added reCAPTCHA conflict handling (v1.3.0): `includes/class-gswp-conflict-guard.php` suppresses other plugins' reCAPTCHA scripts via the `script_loader_tag` filter so this implementation is the only one on the page. New `gswp_conflict_mode` option (`off` / `active` / `site`) surfaced as a "reCAPTCHA Conflict Handling" radio panel (`src/components/Compatibility.jsx`). Matches the Gravity Forms handles from the old manual snippet plus any script whose src loads google.com/recaptcha, generalizing it beyond a single page. `active` mode only strips others where this plugin's own script is enqueued.
- Added PowerPack (Beaver Builder) Login Form integration (v1.3.0): `includes/class-gswp-powerpack.php`. The module fires the core `login_form` / `lostpassword_form` actions (so `GSWP_Login` injects the field) and serializes the whole form with FormData (so the token is sent). Login is validated through the module's `pp_login_form_process_login_errors` filter; lost password has no filter, so its `pp_lf_process_lost_pass` admin-ajax action is guarded at priority 1. `GSWP_Login::inject_field()` now also loads the shared API script + token bootstrap when injecting on a front-end form (not wp-login.php). When login protection is enabled, `replace_module_recaptcha()` (hooked to `fl_builder_render_module_content`) strips the module's own reCAPTCHA/hCaptcha field and dequeues its `g-recaptcha`/`h-captcha` loaders so this plugin's site-wide reCAPTCHA is the only one on the form. Inert unless `BB_PowerPack` is active.

### Files Created/Modified
- [x] [.gitignore](file:///Users/rwaterbury/Developer/google-security-for-wordpress/.gitignore)
- [x] [package.json](file:///Users/rwaterbury/Developer/google-security-for-wordpress/package.json)
- [x] [webpack.config.js](file:///Users/rwaterbury/Developer/google-security-for-wordpress/webpack.config.js)
- [x] [google-security-for-wordpress.php](file:///Users/rwaterbury/Developer/google-security-for-wordpress/google-security-for-wordpress.php)
- [x] [includes/class-gswp-key-scavenger.php](file:///Users/rwaterbury/Developer/google-security-for-wordpress/includes/class-gswp-key-scavenger.php)
- [x] [includes/class-gswp-admin.php](file:///Users/rwaterbury/Developer/google-security-for-wordpress/includes/class-gswp-admin.php)
- [x] [includes/class-gswp-rest-api.php](file:///Users/rwaterbury/Developer/google-security-for-wordpress/includes/class-gswp-rest-api.php)
- [x] [src/index.js](file:///Users/rwaterbury/Developer/google-security-for-wordpress/src/index.js)
- [x] [src/components/App.jsx](file:///Users/rwaterbury/Developer/google-security-for-wordpress/src/components/App.jsx)
- [x] [src/components/SettingsPanel.jsx](file:///Users/rwaterbury/Developer/google-security-for-wordpress/src/components/SettingsPanel.jsx)
- [x] [src/components/PageToggles.jsx](file:///Users/rwaterbury/Developer/google-security-for-wordpress/src/components/PageToggles.jsx)
- [x] [src/components/KeyScavenger.jsx](file:///Users/rwaterbury/Developer/google-security-for-wordpress/src/components/KeyScavenger.jsx)
- [x] [src/components/StatusBadge.jsx](file:///Users/rwaterbury/Developer/google-security-for-wordpress/src/components/StatusBadge.jsx)
- [x] [src/styles/index.css](file:///Users/rwaterbury/Developer/google-security-for-wordpress/src/styles/index.css)
- [x] [includes/class-gswp-frontend.php](file:///Users/rwaterbury/Developer/google-security-for-wordpress/includes/class-gswp-frontend.php)
- [x] [includes/class-gswp-verifier.php](file:///Users/rwaterbury/Developer/google-security-for-wordpress/includes/class-gswp-verifier.php)
- [x] [includes/class-gswp-login.php](file:///Users/rwaterbury/Developer/google-security-for-wordpress/includes/class-gswp-login.php)
- [x] [includes/class-gswp-assets.php](file:///Users/rwaterbury/Developer/google-security-for-wordpress/includes/class-gswp-assets.php)
- [x] [includes/class-gswp-xootix.php](file:///Users/rwaterbury/Developer/google-security-for-wordpress/includes/class-gswp-xootix.php)
- [x] [includes/class-gswp-powerpack.php](file:///Users/rwaterbury/Developer/google-security-for-wordpress/includes/class-gswp-powerpack.php)
- [x] [includes/class-gswp-conflict-guard.php](file:///Users/rwaterbury/Developer/google-security-for-wordpress/includes/class-gswp-conflict-guard.php)
- [x] [includes/class-gswp-gravity-forms.php](file:///Users/rwaterbury/Developer/google-security-for-wordpress/includes/class-gswp-gravity-forms.php)
- [x] [src/components/Compatibility.jsx](file:///Users/rwaterbury/Developer/google-security-for-wordpress/src/components/Compatibility.jsx)
- [x] [readme.txt](file:///Users/rwaterbury/Developer/google-security-for-wordpress/readme.txt)
- [x] [STATE.md](file:///Users/rwaterbury/Developer/google-security-for-wordpress/STATE.md)

### Current Status
- Assets successfully built.
- Gravity Forms reCAPTCHA deferral complete.
- Ready to compile final ZIP plugin package for distribution.
- Ready to push code to GitHub.
